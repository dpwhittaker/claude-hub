# SPEC

## §G GOAL

path-routed reverse proxy + landing page. one local port → multi-project hub. landing, file viewer, ttyd terms, optional per-project app proxy.

## §C CONSTRAINTS

- Node ≥ 22 (marked v18 ESM-only). single process, no framework.
- bind `127.0.0.1` only. external reach via Tailscale (see `.claude/skills/tailscale/`).
- deps locked: `http-proxy ^1.18.1`, `marked ^18.0.3`, `ws ^8.20.0`. CommonJS.
- projects root = `~/projects` (env `PROJECTS_ROOT` overrides).
- proxy port 8002 (env `PROXY_PORT` overrides).
- ttyd unix sockets only — no TCP. socket dir = `/run/ttyd/`, owner=david, mode `0755` (systemd `RuntimeDirectory=ttyd`).
- claude binary = env `CLAUDE_BIN` else `~/.local/bin/claude`.
- systemd-managed. units in `services/`. `RuntimeDirectoryPreserve=yes` required on every ttyd unit.
- `npm` in PATH at hub runtime (vite scaffold + per-project install).

## §I INTERFACES

routes (proxy):
- `GET /` → `landing.html` w/ `lib/tag-filter.js`'s `collectTags`+`hasTag` spliced in at `/* @inject lib/tag-filter.js */` (V72). `?tag=<key>` preselects a chip; `?new=1` opens the create dialog.
- `GET /api/projects` → `{projects: [{name, title, description, tags, openUrl, worktreeOf, branch, createdAt, termUrl, browseUrl}]}` — ordered per V55
- `POST /api/projects` body `{name, github?: {mode: skip|clone|create|onboard, source?, visibility?}, template?: 'none'|'vite'|'game-2d'|'game-3d'|'game-3d-complex'|'jekyll'|'evenhub', firebase?: bool}` → `{name, termUrl, browseUrl}`. `template` defaults `'vite'`, unknown coerced `'vite'`, forced `'none'` when `github.mode ∈ {clone, onboard}` (V43). `firebase` defaults false, forced false when `template==='none'` ∨ clone ∨ onboard ∨ `jekyll` (non-npm) (V45/V52). every mode ends w/ `sudo systemctl enable --now ttyd@<name>.service` (V13). onboard adopts an existing folder under `PROJECTS_ROOT` named `name`; ⊥ clone, ⊥ scaffold on onboard.
- landing dialog field order: Project name → GitHub → Template. GitHub radio order: Clone (default) → Onboard existing folder → Skip → Create. Template radio order: Vite (React + TypeScript) (default, `checked`) | 2D Game (Phaser) | Simple 3D (R3F + Three) | Complex 3D (Babylon) | Jekyll (Markdown site) | Even Hub G2 (glasses + PWA) | None (bare AGENTS.md + README.md + SPEC.md only) — None is LAST, ⊥ first. `Firebase backend` checkbox below radios, disabled + cleared iff template ∈ {none, jekyll} (nothing scaffolded / no `package.json` to merge into — V45, V52). Template fieldset hidden (display:none) when GitHub mode ∈ {clone, onboard}; visible for skip/create.
- `DELETE /api/projects/<name>` → `sudo systemctl disable --now ttyd@<name>.service`, then `extraUnits`, kill tmux, `git worktree remove --force` when the sentinel names a `worktreeOf` parent (V56), rm folder. response `{name, deleted: true, worktreeOf: string|null}`
- `GET /api/view-tree/<proj>` → `{project, tree}`. `?path=<sub>` → one-level lazy `{project, path, entries}`
- `GET /view/<proj>/` → two-pane shell
- `GET /p/<proj>/` → PWA three-pane shell (V57). matches `/p/<name>`, `/p/<name>/`, `/p/<name>/?view=…` ONLY — anything deeper 404s (⊥ shell sub-resources). methods ∈ `{GET, HEAD}` else 405. `?view=` ∈ `{term, open, view, split}`; server-side `initial` ∈ `{term, open, view}` (unknown → `open`). landing cards' Develop action links here (`?view=term`), ⊥ `/term/<proj>/` direct.
- PWA static assets, all from `assets/` via `/assets/<file>` (`^[A-Za-z0-9._-]+$`, `public, max-age=86400`), plus root-scope aliases: `GET /sw.js` + `GET /manifest.webmanifest` (`no-cache`), `GET /favicon.ico|/favicon.png` → `favicon-32.png`, `GET /apple-touch-icon[-precomposed].png`. sw MUST stay at root scope to control the whole site.
- `GET /view/<proj>/<file>` → rendered. `?embed=1` strip chrome. `?raw=1` raw bytes
- `GET /term/<proj>/` → proxy to `unix:/run/ttyd/<proj>.sock` (HTTP + WS upgrade)
- `GET /term/develop/`, `GET /term/shell/` → admin terms (`/term/wsl/` 301s to `/term/shell/`, legacy)
- `GET|*  /<proj>/*` → reverse-proxy if `.project-meta.json` declares `proxyTarget`
- `WS /ws/view-tree/<proj>` → live tree updates `{type: add|delete|change, path, kind?}`
- `GET /api/gh/repos` → `{repos: [{nameWithOwner, description, isFork, isPrivate, updatedAt}]}`. sort: non-forks first then forks; within each group `updatedAt` desc. 503 on `gh` failure. cached in-process, 10 min TTL. response excludes candidates whose basename matches an existing folder under `PROJECTS_ROOT` (managed or not).
- `GET /api/projects/orphans` → `{folders: [string]}`. dirs under `PROJECTS_ROOT` that exist but lack `.project-meta.json` and don't start with `.`.
- `GET /api/term-sessions/<proj>` → `{sessions: [{id, uuid, agent, title}], lastActive: id|null}`. lists current develop-pane tabs in id order (numeric, not lexical). `agent` ∈ `{claude, codex}`; `title` = latest `ai-title` in the claude transcript, `null` for a codex tab (no transcript to read).
- `POST /api/term-sessions/<proj>` body `{agent?}` → `{id, uuid, agent}`. `agent` ∈ `{claude, codex}`, defaults `claude`; unknown → 400 `unknown agent` BEFORE any write or `sudo` (V68). allocates next free `sN`, stamps `{uuid, agent}` into `<proj>/.develop-sessions.json`, `sudo systemctl enable --now ttyd@<proj>__<id>.service`, waits ≤ 5s for the socket. on enable failure → 500 + rolls back the map entry.
- `DELETE /api/term-sessions/<proj>/<id>` → `sudo systemctl disable --now ttyd@<proj>__<id>.service`, `tmux kill-session -t <proj>__<id>`, drops `sessions[<id>]` and clears `lastActive` if it matched.
- `PUT /api/term-sessions/<proj>/active` body `{id}` → writes `lastActive`. 404 if id not in map. ⊥ broadcast — other live devices stay on whatever tab they're on (V49).
- `GET /api/term-capture/<key>` → `{key, cols, rows, lines: string[], pending: {id, kind, payload, createdAt}|null, state: {lastMessage?, lastMessageAt?, notification?, notificationAt?}|null}`. `tmux capture-pane -p -J -t =<key>:` of the VISIBLE pane (V76). polling it = "watching" the key (V75). 400 bad key (V74), 404 no such tmux session.
- `POST /api/term-input/<key>` body `{text: string ≤ 8192, enter?: bool}` → `{ok: true}`. `tmux send-keys -l -- <text>` then `Enter` iff `enter`. how a spoken prompt lands in a session.
- `POST /api/term-scroll/<key>` body `{lines: int}` → `{ok: true, ticks}`. SGR wheel ticks typed into the pane, negative = older (V77), ≤ 200.
- `POST /api/term-pending/<key>` (from `services/glasses-relay-hook.mjs`) body `{kind ∈ question|permission|stop|notification, session_id?, payload}` → `{ok, relay: bool, answer?, reason?}`. `question`/`permission` are HELD (long-poll) until answered/released/watcher lapses/540 s; unwatched → immediate `relay:false` (V75). `stop`/`notification` → recorded into `state`, immediate.
- `GET /api/term-pending/<key>` → `{pending, state}` (same objects as term-capture).
- `POST /api/term-pending/<key>/answer` body `{id, answer: object}` | `{id, release: true}` → `{ok: true}`; 404 nothing pending, 409 stale id. `answer` for a question = `{answers: {<question text>: <label | 'a, b'>}}`, for a permission = `{decision: allow|deny}`.
- `POST /api/stt` body = raw PCM (`audio/L16; rate=16000`, s16le mono) or any container faster-whisper decodes → proxied to `STT_URL/transcribe` → `{text, seconds, ms, device}`. 503 `stt unavailable` when the unit is down (V78).

files:
- `services/claude-hub.service` — proxy unit
- `services/ttyd@.service` — templated per-project term
- `services/ttyd-develop.service`, `services/ttyd-shell.service` — admin terms
- `services/ttyd-attach.sh` — joins/creates tmux session, runs `claude --continue` (gated by V4).
- `services/vite@.service` — templated per-project Vite dev server. `ExecStart=npm run dev`, `WorkingDirectory=~/projects/%i`. enabled on Vite-template bootstrap.
- `services/jekyll@.service` — templated per-project Jekyll preview. `ExecStart=~/projects/%i/serve-local.sh`, `Restart=always`, system PATH (no nvm). enabled on `jekyll`-template bootstrap (V52).
- `templates/vite/` — scaffold source (`package.json`, `vite.config.ts`, `index.html`, `tsconfig.json`, `src/main.tsx`, `src/App.tsx`, `.gitignore`). copied into project on create. `<NAME>` & `<PORT>` placeholders replaced.
- `templates/game-2d/` — Phaser scaffold. vite + TS + `phaser`. `src/main.ts` boots `Phaser.Game` w/ demo Scene (arcade physics, keyboard input, sprite). no React. `<NAME>`/`<PORT>`.
- `templates/game-3d/` — "Simple 3D". vite + React + TS + `three` `@react-three/fiber` `@react-three/drei` `@react-three/rapier` `zustand`. demo: lit scene, `KeyboardControls`, one rapier body, zustand store, drei `Html` HUD. `<NAME>`/`<PORT>`.
- `templates/game-3d-complex/` — "Complex 3D". vite + TS + `@babylonjs/core` `@babylonjs/loaders` `@babylonjs/inspector` + Havok. `src/main.ts`: engine+scene in canvas, Havok physics, inspector toggle on key. `<NAME>`/`<PORT>`.
- `templates/_firebase/` — overlay (⊥ standalone template). `src/firebase.ts` (env-gated Auth+Firestore init), `.env.example` (`VITE_FIREBASE_*`), `firebase.json`, `.firebaserc.template`, README cloud section. copied over base tree when `firebase:true` (V45).
- `templates/jekyll/` — Jekyll/Bundler scaffold (Ruby, NOT vite). `_config.yml` (minima theme), `Gemfile.local` (jekyll+minima+webrick+feed+seo-tag; named `.local` so GH Pages ignores it), `serve-local.sh` (`bundle exec jekyll serve --baseurl /<NAME> --port <PORT>`), `.bundle/config` (`vendor/bundle`), `.gitignore`, `README.md.template` (`permalink: /` → site index), `AGENTS.md.template`. `<NAME>`/`<PORT>` placeholders. scaffolded by `bootstrapJekyll`, not `bootstrapTemplate` (V52).
- `templates/evenhub/` — Even Realities G2 glasses app. vite + TS + `@evenrealities/even_hub_sdk`, NO framework. `src/glasses.ts` = SDK half (`connectBridge` timeout race, `startGlassesPage` → 576x288 text container + event routing, → `null` when no host), `src/main.ts` = phone half (companion UI + `sw.js` registration). `public/manifest.webmanifest` + `public/sw.js` (pass-through, ⊥ cache) + `public/icon.svg` ⇒ installable PWA. `app.json` = Even Hub manifest (`<NAMESLUG>` package id). `<NAME>`/`<PORT>`/`<NAMESLUG>`. vite-family ∴ `bootstrapTemplate` + `vite@<name>.service` (V70).
- `lib/tab-key.js` — `tabKey(p, mode)`. server inlines via `.toString()` into client template.
- `lib/port-alloc.js` — `allocatePort(projectsRoot)` scans sibling `.project-meta.json` for free port ≥ 5173.
- `lib/template.js` — `replaceVars` + `copyTemplate` for scaffold copy w/ `<KEY>` substitution (UTF-8 in/out ∴ template trees are TEXT ONLY — a committed PNG arrives corrupted) + `nameSlug(name)` → `<NAMESLUG>`, the name reduced to lowercase alnum w/ a letter forced in front.
- `lib/project-name.js` — `PROJECT_ID_RE`, `RESERVED_PROJECT_NAMES` primitives. server.js re-exports.
- `lib/bootstrap-prompt.js` — `writeBootstrapPrompt(dir, name, flavor, opts={templateId, firebase})` + `STACK` map (template id → human stack blurb for greenfield prompt).
- `lib/template-policy.js` — `effectiveTemplate(body)` → enum (V43, allowlist + coerce + clone/onboard force) + `firebaseEnabled(body, template)` (V45).
- `lib/gh-repos.js` — `makeGhRepos({exec, ttlMs, now})` cache + `filterReposByFolders(repos, folders)`.
- `lib/onboard.js` — `bootstrapOnboard(dir, name)` + `listOrphanFolderNames(projectsRoot)`.
- `lib/tab-reload-targets.js` — `isEmbedder(path)` + `tabsToReload(tabs, changedPath)`. inlined into client via `.toString()`.
- `lib/file-routes.js` — `matchGlob(glob, path)` + `routeForPath(rules, relPath)`. source path → served-URL (relative to proxy prefix) per `routes` rules. inlined into client via `.toString()`. (V54)
- `lib/escape-html.js` — `escapeHtml(s)`. the one server-side escaper, shared by both page shells and server.js. (the Browse client keeps its own inlined copy — that one runs in the browser.)
- `lib/view-shell.js` — `renderViewShell(project, {proxyPrefix, routes})` → the Browse two-pane document (tree + tabs + develop pane + client script). injects `tabKey`/`installTouchWheel`/`isEmbedder`/`tabsToReload`/`matchGlob`/`routeForPath` via `.toString()`. pure — caller resolves proxyPrefix/routes from the sentinel.
- `lib/pwa-shell.js` — `renderShellHtml(name, openUrl, termUrl, initialView)` → the per-project PWA shell served at `/p/<name>/` (installable, FAB view cycle, split landscape, keyboard fit). pure.
- `lib/split-layout.js` — `makeSplitLayout(minWidth)` → `(width, height) → bool`, the PWA shell's split-layout verdict: `width ≥ minWidth ∧ width > the tallest height seen at that width` — the `(min-width: 900px) and (orientation: landscape)` rule made keyboard-immune (V58, B22). inlined into the shell via `.toString()` (V42). pure.
- `lib/readme-meta.js` — `parseFrontmatter`, `stripInlineMarkdown`, `readmeMetaFromContent(content)`. README text → `{title, description, tags}` for the card. server.js keeps the disk read.
- `lib/project-cards.js` — `buildCard(row)` + `sortCards(cards)` + `buildProjectCards(rows)`. sentinel-over-README precedence, `worktree` badge, worktree-under-parent ordering (V55).
- `lib/worktree.js` — `parentProjectName(meta)`, `branchName(meta)`, `worktreeRemovalPlan({dir, meta, projectsRoot})` → git argv for `worktree remove --force` + `worktree prune` fallback, or null for a plain project (V56).
- the five ttyd-page shims, spliced into `<head>` of every `/term/<key>/` HTML response by `proxyRes` (server.js) and self-contained per V42 — `lib/osc52.js` (`installOsc52Bridge`), `lib/term-reconnect.js` (`installTermReconnect`, V63/V64), `lib/touch-wheel.js` (`installTouchWheel`, V40), `lib/keyboard-fit.js` (`patchViewportMeta` + `installKeyboardFit`, V62), `lib/android-input.js` (`installAndroidInput`, V61). the two WebSocket wrappers are ungated (⊥ `DOMContentLoaded`) and ordered osc52 → reconnect; the rest are gated.
- `lib/scaffold-install.js` — `installCommand({firebase})` + `installEnv(env)`. the command line and environment for a scaffold's `npm install`; both guards of V65 live here, ⊥ in the templates.
- `lib/term-sessions.js` — `readSessionsMap`, `writeSessionsMap`, `allocateTabId`, `parseTermKey`, `joinTermKey`, `TAB_ID_RE`, `TERM_KEY_SEP`, `AGENTS`, `DEFAULT_AGENT`, `isAgent`, `normalizeAgent`. develop-pane tab map io + key parse + the agent validator.
- `lib/term-agents.js` — `TERM_AGENTS` (`[{id,label}]`, the "+" menu's offer order) + `openAgentMenu(anchor, agents, onPick)`. inlined into BOTH shells via `.toString()` ∴ self-contained per V42; the agent list is serialized in as a JSON literal, ⊥ referenced by name.
- `lib/term-relay.js` — `isTermKey`, `makeRelay({now?, watchTtlMs?, holdMaxMs?, sweepMs?})` → `{markWatched, isWatched, hold, answer, release, getPending, getState, setState}`, `wheelSequences(lines)`, `parseCapture(text)`, `buildAnswers(questions, picks)`. pure; the stores behind `/api/term-*` (V74–V77).
- `services/glasses-relay-hook.mjs` — Claude Code hook (PreToolUse:AskUserQuestion, PermissionRequest, Stop, Notification) → `POST /api/term-pending/<tmux session>`; prints a `hookSpecificOutput` ONLY on `relay:true`, else exit 0 silent (V75). env `CLAUDE_HUB_URL`, `GLASSES_RELAY=0` kill-switch.
- `services/install-glasses-hooks.mjs` — merges those four entries into `~/.claude/settings.json` by command string (idempotent); `--remove` takes exactly them out. env `CLAUDE_SETTINGS` overrides the file.
- `services/stt/server.py` + `services/stt.service` — faster-whisper `turbo` HTTP service on `127.0.0.1:8012` (`POST /transcribe`, `GET /health`), venv `~/stt-env` (uv py3.12: `faster-whisper nvidia-cublas-cu12 nvidia-cudnn-cu12`, `LD_LIBRARY_PATH` to the two wheel lib dirs in the unit). gpu-gate lease owner `stt` while the model is on the GPU; idle 90 s → unload + release; CPU int8 fallback (V78).
- `<project>/.develop-sessions.json` — `{sessions: {sN: {uuid, agent}}, lastActive: sN|null}`. sole source of truth for develop-pane tabs; read by `ttyd-attach.sh` to launch the tab's agent. bare-string value `{sN: uuid}` is the pre-agent shape ⇒ reads as `{uuid, agent:'claude'}`, rewritten into the object form on the next write (V68).
- `eslint.config.js` — flat config (`@eslint/js` recommended + node globals).
- `.github/workflows/ci.yml` — push/PR trigger; `npm ci` + `npm run lint` + `npm test` on Node 22.
- `<project>/.project-meta.json` — sentinel. fields: `name, createdAt, openUrl?, proxyTarget?, proxyPrefix?, stripPrefix?, extraUnits?, template?, routes?, title?, description?, worktreeOf?, branch?` (`routes` = ordered `[{match, to}]` rewrite rules, V54; `title`/`description` override README, `worktreeOf`/`branch` mark a git worktree, V55/V56)
- `<project>/README.md` — H1 = card title, first para = description, frontmatter `tags: [...]` = badges
- `<project>/AGENTS.md` — agent context (per project)
- `<project>/vite.config.ts` (Vite template) — `base: '/<name>/'`, `server.port: <port>`, `server.host: '127.0.0.1'`

env:
- `PROXY_PORT` (default 8002)
- `PROJECTS_ROOT` (default `~/projects`)
- `CLAUDE_BIN` (default `~/.local/bin/claude`)
- `TTYD_BIN` (default `ttyd`)
- `GIT_AUTHOR_NAME` (optional; passed as `git -c user.name=...` during `bootstrapCreateRepo` / `ghInitPush`. empty = let git use global config)
- `GIT_AUTHOR_EMAIL` (optional; same)
- `TERM_WATCH_TTL_MS` (default 5000) — how long after its last `/api/term-capture` poll a terminal counts as watched (V75)
- `STT_URL` (default `http://127.0.0.1:8012`) — the whisper service `/api/stt` proxies to

module exports (test surface, not public API):
- `server` — `http.Server` instance, lazy-listened (guard: `require.main === module`)
- `PROJECT_ID_RE`, `RESERVED_PROJECT_NAMES` — name validation primitives
- `projectWatchers` — `Map<project, { watcher, clients, pending, dimRefresh }>`. tests probe `.has(project)` for teardown checks.

v2 routes (T93 — the `/v2/` shell, served beside v1; state in `HUB_STATE_DIR` = `~/.claude-hub`, env override):
- `GET /v2/` → `v2/index.html`; `/v2/<asset>` → `v2/` files (no-cache); `/v2/lib/v2-layout.js` → `lib/v2-layout.js` wrapped as `window.HubLayout`.
- `GET|POST /api/v2/profiles` → `{profiles:[{id,name,color,createdAt,updatedAt,rev,tabCount}]}` / create `{name, color?, instructions?}` → profile. `GET|PUT|DELETE /api/v2/profiles/<id>` → `{id,name,color,rev,layout,tabs,instructions}`; PUT `{layout?, tabs?, name?, color?, instructions?, rev?}` — stale `rev` → 409 `{error, rev}`; layout+tabs validated together (V82). disk: `profiles/<id>/profile.json` + `CLAUDE.md`.
- `GET /api/v2/sessions` → `{sessions:[{id, kind:'hub'|'legacy', cwd, agent, uuid, profile, title, termKey, termUrl, running}]}` (legacy = every `<proj>__sN` from `.develop-sessions.json`). `POST` `{cwd, agent?: claude|codex|shell, profile?, title?, prompt?}` → session (`termUrl` = `/term/hub/?arg=<id>`). `PATCH /api/v2/sessions/<id>` `{title}`; `DELETE` kills `tmux =hub-<id>` + removes the record. disk: `sessions/<id>.json` (+ `.prompt`).
- `GET /api/v2/services` → `{services:[{unit,title,description,active,sub,enabled,pid,since,project,url}], tailnet:[{url,scope,mounts:[{path,mode,target}]}]}`. `POST /api/v2/services/<unit>/(start|stop|restart)` (`sudo -n systemctl`), `GET …/logs?n=` → `{unit, lines}` — unit ! ∈ discovered set else 404 (V85). overrides: `<HUB_STATE_DIR>/services.json` `{"<unit>": {url?, title?}}`.
- files (`?path=` relative to `PROJECTS_ROOT`, V79): `GET /api/v2/fs/list` → `{path, repo, entries:[{name,kind:dir|file,path,size,mtime,dim,git,repo?,project?,fileKind?,modes?,defaultMode?}]}`; `GET …/stat` → `{…, fileKind, modes, defaultMode, mime, lang, textual, tooLarge, git, previewUrl}`; `GET …/text` → `{content,size,mtime,lang,…}` (≤ 2 MB else 413); `PUT …/text` `{path, content, baseMtime?}` → `{mtime,size}` | 409 `{error, mtime}` (V81); `GET …/raw[&download=1]` bytes; `GET …/render` markdown → html page (relative img/links → raw); `GET …/diff?ref=&to=` → `{repo, ref, to, diff}`; `GET …/log?n=` → `{repo, commits:[{sha,short,date,author,subject}]}`; `GET …/show?ref=` → `{content}`; `POST …/mkdir|create|rename` `{path[, to]}`.
- `POST /api/v2/ai/complete` `{path?, lang?, before, after}` → `{text, model}` via `claude -p --model $HUB_COMPLETION_MODEL` (default `claude-haiku-4-5-20251001`).
- term: `ttyd-hub.service` binds `/run/ttyd/hub.sock` with `--url-arg`; `/term/hub/?arg=<id>` → `ttyd-attach-hub.sh <id>` → tmux `hub-<id>` (V84). `/api/term-*` relay routes take `hub-<id>` keys unchanged.

## §V INVARIANTS

- V1: ∀ req → URL path validated before disk access. file paths ! `startsWith(projectRoot + sep)` else 400.
- V2: project name ! match `PROJECT_ID_RE` & ∉ `RESERVED_PROJECT_NAMES` & ! starts with `.` else 404.
- V3: `/view/*` methods ∈ `{GET, HEAD}` else 405.
- V4: `ttyd-attach.sh` per-tab path (`<project>__<tabId>`) launches `claude --session-id <uuid>` first time / `--resume <uuid>` thereafter (uuid from `.develop-sessions.json` — see V48). legacy bare-key path keeps the original `--continue` gate (only passes `--continue` when `~/.claude/projects/<encoded>/*.jsonl` exists). either path ⊥ exit-loop.
- V5: every ttyd unit ! carry `RuntimeDirectoryPreserve=yes`. ⊥ shared `/run/ttyd/` wipe.
- V6: file render size ≤ 2 MB, else 413 + suggest `?raw=1`.
- V7: tree node count ≤ 5000 per walk. dim dirs not recursed eagerly.
- V8: `.project-meta.json` required as sentinel for managed-project listing & deletion.
- V9: WS file-watcher events ! filter dim paths (gitignored + hidden dirs). ⊥ noise from `node_modules` builds.
- V10: WS `change` event fired iff path ∈ `knownFiles` at watcher seed. else `add` & insert into `knownFiles`.
- V11: viewer iframe reload on `change` ! preserve scroll via `contentWindow.scrollTo(prevX, prevY)` after `load`.
- V12: dir delete → drop descendants from `knownFiles` & `knownDirs` (Linux recursive watch may skip per-child).
- V13: project create → `mkdir` + `.project-meta.json` + `README.md` + `AGENTS.md` + `sudo systemctl enable --now ttyd@<name>` ! all atomic. partial fail → cleanup.
- V14: WS upgrade `/ws/view-tree/<proj>` handled in-process (`viewTreeWss`). proxy upgrade only after non-match.
- V15: tab state per project keyed by `mode + '\0' + path` (NUL separator — only byte forbidden in POSIX paths). localStorage `view-shell:tabs:<proj>` + `view-shell:active:<proj>`. ⊥ cross-project bleed. ⊥ collision w/ filenames containing mode-prefix string.
- V16: eye-icon tab. gate = `isRenderable(name)` = `isHtmlFile ∨ isSvgFile`. HTML: project w/ `.project-meta.json`.`proxyTarget` → iframe = `<proxyPrefix>/<relpath>` (index.html at any depth → trailing slash, let upstream serve its own root); project w/o `proxyTarget` → `?raw=1`; RAW_MIME[.html] = `text/html; charset=utf-8`. ⊥ raw bytes of build-tool entry-point index.html (Vite source template refs absolute `/src/main.tsx`, browser can't transpile, assets 404 against wrong origin → white screen). ⊥ octet-stream fallback for `.html`. SVG: render ALWAYS `?raw=1` even w/ `proxyTarget` (proxy branch gated by `isHtmlFile(filePath)`) — RAW_MIME[.svg] = `image/svg+xml`, served inline (no Content-Disposition), browser renders natively; `.svg` is a source file, ⊥ proxy app entry point.
- V17: Vite upstreams ! `server.allowedHosts` covering the tailnet host, else 403 `Blocked request` — proxy forwards the original `Host`. use suffix wildcard `['.ts.net','localhost','127.0.0.1']` (⊥ committing a hostname). ⊥ verifying via `127.0.0.1:8002` alone — loopback passes while the tailnet URL 403s. (superseded: this slot previously recorded a WSL2 self-loopback failure; host is native Linux, self-loopback works.)
- V18: ⊥ orphan `node server.js` binding 8002 — systemd owns it. fix on EADDRINUSE: `pkill -f 'node.*server.js'` then `systemctl restart claude-hub.service`.
- V19: project HTTP backends ! concurrent (`ThreadingHTTPServer` or async). stock `http.server.HTTPServer` single-threaded → `CLOSE_WAIT` pile, wedge.
- V20: Vite-style upstreams ! `base: '/<name>/'` + `.project-meta.json` `stripPrefix: false`. else asset URLs collapse.
- V21 (revised): project create default `template: 'vite'` (React + TS). `template: 'none'` = bare `AGENTS.md` + `README.md` + `SPEC.md` — ⊥ "AGENTS+README only", the pre-V67 set: every project carries a spec, bare included.
- V22: per-project Vite port = free port ≥ 5173, allocated by in-process scanner before scaffold. persisted as `proxyTarget` in `.project-meta.json`. ⊥ collision.
- V23: Vite template scaffold ! stamp `.project-meta.json` w/ `proxyTarget: 'http://127.0.0.1:<port>'`, `proxyPrefix: '/<name>'`, `stripPrefix: false`, `openUrl: '/<name>/'`, `template: 'vite'`, `extraUnits: ['vite@<name>.service']`. enforces V20.
- V24: Vite scaffold post-step → `npm install` + `sudo systemctl enable --now vite@<name>.service`. cleanup-on-fail per V13.
- V25: `services/vite@.service` ! carry `Restart=always`, `RestartSec=2`. dev server crash → auto-recover w/o user touching systemd.
- V26: project deletion ! stop `vite@<name>.service` via `extraUnits` before rm. ⊥ orphan port held.
- V27: WS reconnect → force-reload every open tab (cache-bust). recover from `change` events missed during disconnect window. preserve scroll per V11.
- V28: per-tab scroll position persisted to `view-shell:scroll:<proj>:<key>` localStorage. restored on iframe `load`. survives page refresh + tab close/reopen.
- V29 (revised): `github.mode === 'clone'` preserves cloned tree verbatim. `AGENTS.md` / `README.md` / `SPEC.md` written **only if missing** — by the first session per V31, ⊥ pre-filled w/ boilerplate. ⊥ overwrite of pre-existing docs. SPEC.md joined the set at V67; on clone it is DISTILLED from the tree, ⊥ stamped from a skeleton (nothing to distill on greenfield, so that path stamps).
- V30: (merged into V31).
- V31 (revised): bootstrap prompt branches, written to `<project>/.claude-bootstrap.txt` and consumed by `ttyd-attach.sh` `tmux send-keys` (read + send + delete):
  - **scan-existing** (clone / onboard): Claude walks tree first turn and writes whichever of `README.md` (human-facing — purpose + "what is this & why"), `AGENTS.md` (agent-facing — tech stack, conventions, directory layout, debugging signposts) or `SPEC.md` (DISTILLED from the tree per V67 — §I from the public surface, §V from existing tests/assertions, inferences flagged `?`) is missing. ⊥ overwrite per V29.
  - **greenfield** (skip / create + scaffold): Claude reads AGENTS.md+README.md+SPEC.md, greets naming the scaffolded stack so it's oriented, asks "what should we build here?". then fills §G/§C in `SPEC.md` & flips §T.1 (V67). prompt names the template stack (`writeBootstrapPrompt` opts `{templateId, firebase}` → §I `STACK` map); ⊥ blank greet w/ no idea it's a Phaser/R3F/Babylon project. template `none` / no opts → no stack line.
- V32: `/api/gh/repos` runs `gh repo list --json nameWithOwner,description,isFork,isPrivate,updatedAt --limit 200`. result cached in-process w/ ≤ 10 min TTL. ⊥ shell-out per dialog open. response excludes any candidate whose basename matches an existing folder under `PROJECTS_ROOT` (managed or not). sort: `isFork=false` first then `isFork=true`; `updatedAt` desc within each group. ⊥ user's own forks dominate top of dropdown.
- V33: dialog clone-source is a `<select>` populated async from `/api/gh/repos`. on fetch failure / empty / timeout → fall back to free-text `<input>`. ⊥ block dialog while waiting.
- V34: cloning another user's repo = fork on github.com first, pick the fork from the dropdown. arbitrary-URL clone still possible via direct `POST /api/projects` w/ `source: 'owner/repo'` (power-user flow), but not via the dialog.
- V35: Clone is the default GitHub mode in the create dialog. Default dialog open state → Template fieldset hidden.
- V36: `github.mode === 'onboard'` adopts an existing folder under `PROJECTS_ROOT`. Stamps sentinel `.project-meta.json` w/ `name + createdAt` only (no `github`, no `template`). Writes scan-existing bootstrap prompt. 409 if `.project-meta.json` already exists. 404 if folder missing. ⊥ overwrite of any existing file in the tree. ttyd@ enable per V13.
- V37: dialog onboard mode populates `<select>` from `/api/projects/orphans`. Empty list → mode option disabled w/ hint "no orphan folders under ~/projects".
- V38: develop pane sits at `<main>` level — sibling of the (tree-pane | work-area) row — so it spans the full `<main>` width when shown. ⊥ nested inside `work-area` (would clip terminal to area below the file-tabs row). `<main>` is column flex; persisted as `view-shell:develop-height:<proj>`.
- V39: `ttyd-attach.sh` `CLAUDE_BIN` default ! match §C claude-binary default (`$HOME/.local/bin/claude`). bare `claude` ⊥ resolve in systemd PATH (`/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/snap/bin`) → tmux command fails → session dies → V4 exit-loop trip.
- V40: develop-pane terminal iframe ! translate touch-drag → synthetic `wheel` events on iframe document so xterm scrolls under finger drag. attach on iframe `load` (same-origin via proxy). QUANTIZED: accumulate drag px, emit 1 wheel per 1-text-line step (`step = viewportHeight/term.rows`, fallback 16px), deltaY = ±step. rationale: tmux (mouse on) puts outer xterm in alt buffer w/ zero local scrollback → every wheel = 1 mouse tick to tmux regardless of deltaY; per-touchmove dispatch @60Hz ≈ 60 ticks/s overshoot. tick lands in inner app (Claude Code transcript scroll — it requests mouse tracking, tmux default WheelUpPane passthrough MUST stay, see B14) or tmux copy-mode (5 lines/tick) for non-mouse apps. step ! match the CONSUMER's lines/tick, and the consumer for the primary case (Claude session) is claude @ **1 line/tick** → 1-line step = finger↔content 1:1. ⊥ sizing off tmux copy-mode's 5 (see B15). raw shell panes → copy-mode ⇒ 5 lines per line of travel (accepted, secondary case). preventDefault every single-touch move (⊥ rubber-band). ⊥ stuck/overshoot terminal scroll on mobile/tablet.
- V41: ∀ WS `change` event → reload tabs where `info.path === path` ∨ tab doc is embedder (ext ∈ {`md`, `markdown`, `html`, `htm`}). HTML/MD transitively embed `<img>`/`<script>`/`<link>`; child-asset change ⇒ parent tab must cache-bust. ⊥ stale embedded asset until tab close+reopen. scroll preserved per V11.
- V42: any `lib/*.js` function inlined into client via `.toString()` ! reference module-scope consts (closure dies on inline → `ReferenceError` at runtime). literals must live inside the function body. test must round-trip via `new Function(src)()` to prove self-containment.
- V43 (revised): `template` ∈ enum `{none, vite, game-2d, game-3d, game-3d-complex, jekyll, evenhub}` — ⊥ the pre-T87 6-member enum. id == `templates/<id>/` dirname (1:1). unknown → coerce `vite`. clone/onboard force `none` (extends V21). every non-`none` template EXCEPT `jekyll` = vite project (`npm run dev`) → reuses `vite@<name>.service` (V25), port alloc (V22), proxy meta (V23); `evenhub` included (V70). `jekyll` is the one non-vite family — own scaffolder + unit (V52). browse/eye-icon (V16/V53) works unchanged via `proxyTarget`.
- V44: `bootstrapTemplate(dir, name, templateId, {firebase})` generalizes bootstrapVite — copy `templates/<templateId>` → stamp `.project-meta.json` w/ V23 fields but `template: <templateId>` → optional firebase overlay (V45) → `npm install` → enable `vite@<name>`. cleanup-on-fail per V13. `vite` = the identity case.
- V45: `firebase:bool` POST field. forced false when `template==='none'` ∨ clone ∨ onboard (no scaffold to inject). when true → `copyTemplate(templates/_firebase)` over project tree THEN `npm install firebase` (npm merges `package.json` — ⊥ JSON-merge via placeholder copy). overlay files per §I. dialog checkbox enabled iff template ≠ None.
- V46: game + vite templates ship two static-deploy build scripts — `build:pages` (`vite build --base=/<NAME>/`, GH Pages) + `build:firebase` (`vite build --base=/`, Firebase Hosting). dev base stays `/<name>/` per V20 (proxy routing). ⊥ single base serving both hosts. GH Pages workflow + `firebase.json` shipped (latter via `_firebase` overlay).
- V47 (revised): develop pane = N tabs, each tab = `ttyd@<proj>__sN.service` + tmux session named `<proj>__sN` + an AGENT (V68) and a uuid naming that agent's conversation. id format `sN` (positive integer, no leading zero). `<proj>/.develop-sessions.json` `{sessions:{sN:{uuid,agent}}, lastActive:sN}` is sole source of truth. ⊥ the bare `{sN:uuid}` value it replaced — still READ as a claude tab, never written. `allocateTabId` reuses gaps (smallest unused) so labels stay compact. project create stamps `s1` with a fresh uuid + `agent:'claude'` before enabling the unit.
- V48 (revised): `ttyd-attach.sh` splits its arg on first `__` → `<project>__<tabId>`. tabId = `sN` → read `.develop-sessions.json` for `{uuid, agent}`, launch per V69. agent `claude`: `claude --resume <uuid>` iff `~/.claude/projects/<encoded>/<uuid>.jsonl` exists else `claude --session-id <uuid>`. no separator → legacy bare-key path (kept for admin units `develop`/`shell` and any unmigrated project), always claude. bootstrap-prompt only fires on s1 (or legacy bare) so secondary tabs start clean. the two probes are sed over the pretty-printed map: `/"sN"[[:space:]]*:[[:space:]]*{/,/}/p` isolates ONE tab's block, then `uuid`/`agent` are read out of it — ⊥ a file-wide `"sN": "…"` probe, which with a second map keyed by the same ids would return whichever came first in the file. the file-wide form survives ONLY as the pre-agent fallback, reached when the block probe finds nothing.
- V49: PUT `/active` writes server-side only; ⊥ broadcast to other live connections. switching tabs on one device must not yank others. new connections read `lastActive` on load → start there. ⊥ localStorage for active id (cross-device coherence).
- V50: server startup migrates legacy `ttyd@<project>.service` units (no `__`) to `ttyd@<project>__s1.service`. extracts existing claude uuid from latest `~/.claude/projects/<encoded>/*.jsonl` (else fresh uuid), writes map, `tmux rename-session <project> → <project>__s1` (preserves live process + convo), disables old unit, enables new. idempotent — skips when `.develop-sessions.json` already exists. admin keys `develop`/`shell` exempt (`wsl` retained in the set as the pre-rename name).
- V51: project DELETE enumerates `ttyd@<project>__*.service` via `systemctl list-units` (all states), disables every child + legacy bare unit in one batch, kills every matching tmux session, then removes the directory. ⊥ orphan ttyd units after a delete.
- V52: `jekyll` template = Ruby/Bundler, not vite. `bootstrapJekyll(dir, name)` (not `bootstrapTemplate`): copy `templates/jekyll` → `chmod +x serve-local.sh` (copyTemplate writes 0644; unit execs it) → stamp meta `template:jekyll, proxyTarget:127.0.0.1:<port>, stripPrefix:false, openUrl:/<name>/, extraUnits:[jekyll@<name>.service]` → port via `allocatePort(root, 4000)` (4000s range, ⊥ collide w/ vite 5173+) → `BUNDLE_GEMFILE=Gemfile.local bundle install` (project-local `vendor/bundle`) → `sudo systemctl enable --now jekyll@<name>.service`. cleanup-on-fail per V13. `scaffoldProject` dispatches jekyll→bootstrapJekyll else→bootstrapTemplate. firebase forced off (no package.json). DELETE drops `jekyll@<name>` via the generic `extraUnits` path.
- V54: `.project-meta.json`.`routes` = ordered `[{match, to}]` rewrite rules mapping a source file's repo-relative path → the URL it renders at (relative to `PROXY_PREFIX`). glob: `*`=one segment, `**`=zero+ segments (captured `:splat`); `to` template vars `:dir :name :ext :path :pathnoext :splat`; output slash-normalized (`//`→`/`, `#/`→`#`); first match wins; any path segment starting `_`/`.` ⊥ routed. `lib/file-routes.js` (`matchGlob`+`routeForPath`) inlined into the Browse client. `readProjectRoutes` validates server-side: `match`/`to` strings ≤256, `to` matches `^/[^\s"'<>\\]*$` (root-relative, no scheme/host/CRLF) else dropped. Browse shows the preview eye-icon when `isRenderable(name) ∨ (PROXY_PREFIX ∧ routeForPath)`; render iframe src = `PROXY_PREFIX + encodeURI(route)` (takes precedence over the html-proxy + raw branches). expresses real Jekyll (genesis: `README→/`, `**/index.md→/:dir/`, `**/*.md→/:dir/:name.html`) AND `.nojekyll` SPA (systematic-theology: `data/**/*.md→/#:splat/:name`, `handouts|storyboards/**/*.md→/#:path`). jekyll template stamps the genesis-style default rules. ⊥ route render without a running backend (needs PROXY_PREFIX).

- V55 (revised): landing card `title`/`description` = `.project-meta.json` value if set, else README (H1 / first para), else folder name / `''`. a sentinel with `worktreeOf` (validated project name) gets a leading `worktree` tag ahead of its PARENT CARD's tags (`inheritWorktreeTags`) — ⊥ its own README's: the branch checkout carries whatever README that branch has, so a retag of the parent on main would leave every worktree card advertising the old tags (and minting chips for them, V73) until the branch merged. parent absent from the hub → own README tags. surfaces `worktreeOf` + `branch` on the card. ordering: creation order, but a worktree sorts under its PARENT's `createdAt` and immediately after it (parent first, then siblings alphabetical); a worktree whose parent is absent falls back to its own `createdAt`. ⊥ README-only cards — a `git worktree add` checkout inherits the parent README byte-for-byte, so README alone makes every worktree card a duplicate of its parent. `lib/project-cards.js` owns both; `lib/readme-meta.js` owns the README parse.
- V56: DELETE on a project whose sentinel names `worktreeOf` runs `git -C <parentDir> worktree remove --force <dir>` BEFORE the rm — the checkout is registered in the parent repo's `.git/worktrees/`, and rm alone strands that entry (parent keeps listing a gone checkout & refuses to reuse the path). on git failure → rm, then `git -C <parentDir> worktree prune` so the parent ends consistent either way. `worktreeOf` is on-disk JSON → validated with `PROJECT_ID_RE` + no leading `.` before it becomes a path; invalid ⇒ treated as a plain project (⊥ traversal out of `PROJECTS_ROOT`). `--force` is deliberate: the UI already confirmed, a dirty scratch checkout must not strand the folder half-removed.
- V57: PWA shell (`/p/<proj>/`) = THREE sibling panes — `#pane-term` (tab strip + ttyd iframes), `#pane-open` (`openUrl`), `#pane-view` (`/view/<proj>/`). each owns ONE fixed src for the life of the page; ⊥ swapping the OPEN pane's src by layout (the old split-capable⇒browse / phone⇒openUrl flip) — a rotation would re-navigate a live pane. FAB single-pane rotation is ALWAYS `term → open → view`. `split` is a MODE, ⊥ a step in that rotation — while split, a FAB tap swaps the right half (`open ↔ view`) and STAYS split; entering/leaving is the long-press menu's job alone (V58). ⇒ `splitEnabled()` ⇔ `currentView === 'split'` at all times. FAB label = the NEXT view, not the current one (while split: the other right-hand pane). `split` = `#pane-term` at 50% left + whichever of open/view was last single-paned (`rightView`, default `view`) at 50% right; only `.pane.active` is painted (⊥ blanket `body.split .pane{visibility:visible}` — with 3 panes it stacks the idle right pane over the live one). idle panes mount on an 800ms + 700ms stagger after first paint. `?view=` ∈ `{term, open, view, split}`; server-side `initial` ∈ `{term, open, view}` (unknown → `open`).
- V58: split is a per-device, per-PROJECT preference in `localStorage` `claude-hub:split:<proj>` (`'1'`/`'0'`), set from the FAB long-press menu. it OVERRIDES the layout verdict in BOTH directions — a phone can be forced into split, a landscape tablet forced out. unset ⇒ the layout decides: `lib/split-layout.js` — `width ≥ 900 ∧ width > the tallest height this width has been seen at`, re-judged on every `window` `resize`, acting only on a CHANGED verdict. ⊥ the live `(min-width: 900px) and (orientation: landscape)` media query it replaced: the shell's viewport meta is `interactive-widget=resizes-content`, so the soft keyboard shrinks the LAYOUT viewport, and `orientation` is a layout-viewport feature (landscape ⇔ width > height) — on a portrait tablet ≥ 900px wide it flipped every time the keyboard opened (B22). a keyboard only takes height; a rotation always changes width ⇒ a fresh width is judged by its first (keyboard-free) height, a shorter height at a known width IS the keyboard, and a taller one (the keyboard closing after a rotation made with it up) replaces the memory — always right with the keyboard down. accepted desktop corner: a height-only window resize keeps the previous verdict until the width next moves. only while unset does a rotation across the boundary move the view: INTO a split layout ⇒ enter `split` (the FAB can no longer get you there), out of one ⇒ drop `split` → `rightView`. the menu toggle is the sole in/out: on ⇒ enter split immediately, off ⇒ drop to `rightView`.
- V59: FAB long-press (550ms, ⊥ after DRAG_SLOP movement) opens a menu, not a bare refresh. items = `Refresh <PANE>` (targets the visible pane — `rightView` in split — so a stray press never drops the terminal's ttyd attachment; one-shot `?__r=<ts>` cache-bust, `.src` re-assign not `contentWindow.location.reload()`) + a checkable `Split view` (V58). a full-viewport scrim under the menu is MANDATORY: every pane is an iframe, so an outside tap would otherwise never reach the shell document. the synthetic click that follows the long-press is consumed by `suppressClick` BEFORE the menu-dismiss branch — else the menu closes the instant it opens.
- V60: the term tab strip carries a home link (`href="/"`) as its FIRST child, `position:sticky; left:0` so the strip's own x-scroll can't hide it. an installed PWA has no browser chrome — this strip is the only route back to the hub. (the Browse shell has its own header home link; ⊥ duplicate one there.)
- V61: Android soft-keyboard input is taken off xterm's `CompositionHelper` by `lib/android-input.js`, injected into every ttyd page beside touch-wheel/osc52/keyboard-fit. UA-gated (`/Android/i`) — inert on desktop + iOS, which keep xterm's stock path. mechanism: xterm binds `compositionstart|update|end` on the textarea in the BUBBLE phase and `keydown|keypress|input` capture ON THE TEXTAREA, so a capture listener on `document` runs first and `stopImmediatePropagation` suppresses xterm's handling with no private-field patching. replaces the deferred diff with a SYNCHRONOUS prefix-diff of `term.textarea.value` against a mirror of what was last sent, emitting one `\x7f` per dropped CODEPOINT (⊥ per UTF-16 unit — one backspace erases an emoji) then the inserted tail (`\n`→`\r`). ⊥ setTimeout, ⊥ `_isComposing` gate: a burst of keydowns, or a composition opening mid-burst, cannot lose a character. EXACTLY ONE SENDER PER KEY, decided at keydown by `xtermOwns`: text keys are the shim's (keyCode 229 AND every printable `key`, space included) and are suppressed at BOTH `keydown` and `keypress` — `stopImmediatePropagation` stops propagation but NOT the default action, so Chrome still fires `keypress` and xterm's own `_keyPress` listener would send the char on top of the diff (B18); control keys (Enter/Tab/arrows/Esc/Backspace/Home/End/Fn, anything with ctrl|alt|meta) stay xterm's and the textarea change they cause is ADOPTED into the mirror, ⊥ re-sent. `compositionstart` reclaims ownership from a pending xterm key; `focusin` RE-seeds the mirror (⊥ only on first sight of the element) and clears `composing`/`adoptNextInput`, which does two jobs: a keydown-less `input` (voice, suggestion-strip tap) is diffed rather than swallowed by first-sight seeding, AND the mirror is resynced after xterm's `_handleTextAreaBlur` empties the box (`this.textarea.value = ''`) under a shim that would otherwise keep diffing against the pre-blur text and emit one DEL per stale char (B23). the pairing is exact — xterm clears on the element's `blur`, we re-seed on its `focusin`, and nothing can type into an unfocused textarea, so whatever it holds when focus lands is neither ours to send nor ours to delete. the textarea is NOT emptied per keystroke (Gboard only emits `deleteContentBackward` when there IS something to delete, so an empty box eats backspaces) — trimmed to a 64-char tail past 512, and ONLY at a whitespace boundary (⊥ rewriting the box mid-word — Gboard tracks its composing region by offset and desyncs, B18) with a 2048 hard cap as the escape hatch. uses only public xterm API: `term.textarea`, `term.input(data, true)`, `term.scrollToBottom()`. self-contained per V42.
- V62: `installKeyboardFit` ! forward a visualViewport 'resize' that changed neither height nor width, and ! coalesce the synthetic `window` 'resize' to one per rAF. ttyd binds `window resize → fitAddon.fit()` UNDEBOUNCED and a fit() landing on new rows/cols sends RESIZE_TERMINAL ⇒ SIGWINCH ⇒ full TUI repaint; Gboard fires vv 'resize' for no-op strip toggles while you type, so forwarding each one burns a full redraw per keystroke (B17). no rAF ⇒ dispatch stays synchronous.
- V63: ttyd terminals reconnect themselves. ttyd 1.7.7 registers `error → doReconnect = false` on its socket, and a real network loss (phone sleeping, wifi↔cellular handoff, tailnet blip) fires `error` before `close` — so `onSocketClose` ALWAYS took the manual `Press ⏎ to Reconnect` branch; the automatic one was reachable only on an error-free close, which is not how a network goes away. `installTermReconnect` (`lib/term-reconnect.js`, head-parse inject AFTER `installOsc52Bridge` — both wrap `window.WebSocket`, and this one must see the socket osc52 returns) drops ttyd's `error` listener and HOLDS its `close` listener, calling it after a backoff: ttyd reconnects the moment it hears the close, so delaying delivery IS the backoff. 0ms after a session that stayed open ≥5s (a real drop), else 1s→2s→4s→8s→15s cap; page `hidden` ⇒ hold indefinitely and fire on `visibilitychange`, streak reset (⊥ burning retries a sleeping phone can't see). code 1000 passes through UNCHANGED — a clean server-side close means the command exited, and retrying that loops a fresh attach. ⊥ ttyd's own timers patched.
- V64: on reopen, refit — ttyd's `dispose()` runs BEFORE the reconnect and takes `initListeners()`'s `window 'resize' → fitAddon.fit()` binding with it, so nothing fits while the socket is down and `onSocketOpen` re-handshakes `{columns: terminal.cols, rows: terminal.rows}` with the pre-drop pair. rotate/keyboard/pane-resize while disconnected ⇒ session returns the wrong shape and STAYS wrong (the listener it re-registers needs a resize event that already fired). fix = call `window.term.fit` (set in ttyd's `open()`, never torn down) on a 0ms + 300ms stagger after `open`, so ttyd's `onSocketOpen` has re-registered `onResize → RESIZE_TERMINAL` and a wake-up rotation has had a frame to settle. ⊥ a synthetic `window` resize as the primary path — V62's dedupe drops one when neither viewport dimension changed, which is exactly the case that still needs a refit (the stale dimensions are xterm's, not the viewport's); kept only as a fallback. `FitAddon.fit()` no-ops on unchanged dims, so an extra refit is free.
- V65: the scaffold's `npm install` ! run with `NODE_ENV=production`. `services/claude-hub.service` sets it, the install is a CHILD of the hub and inherits it, and npm reads it as `--omit=dev` ⇒ `dependencies` install and every devDependency (vite, typescript, `@vitejs/plugin-react`, `@types/*`) is skipped — while npm STILL EXITS 0, so `bootstrapTemplate`'s catch/`rmSync` never fires and the project scaffolds "successfully" into something `vite@<name>.service` then crash-loops on (`sh: 1: vite: not found`, B20). TWO guards, ⊥ one: `lib/scaffold-install.js` `installEnv(process.env)` pins `NODE_ENV=development` (matching `services/vite@.service`) AND `installCommand()` appends `--include=dev` to EVERY npm install it emits (the firebase overlay's second install included — `npm install <pkg>` re-resolves the whole tree and a production-flavoured second pass prunes what the first placed). the flag is ⊥ redundant: the install runs through `bash -lc`, a LOGIN shell that sources the user's profile AFTER the env is handed over, so a profile exporting `NODE_ENV` beats `installEnv` and only the command line survives it.
- V66: every vite-family template's `vite.config.ts` ! declare `server.allowedHosts` covering `.ts.net`. the hub proxies with `changeOrigin: false`, so the upstream sees the CLIENT's `Host`; Vite 7 403s (`Blocked request. This host … is not allowed.`) any host not on the list. loopback tests pass while the tailnet URL fails, so this is invisible until the real link is opened (B21). the entry is the leading-dot SUFFIX WILDCARD `'.ts.net'` — ⊥ a concrete FQDN, which would commit this machine's tailnet name to the repo.
- V67 (revised): SDD is the working method, defined ONCE in `SDD.md` at this repo's root — ⊥ duplicated per project; templates LINK to `~/projects/claude-hub/SDD.md`. format adapted from cavekit's `FORMAT.md` (`github.com/JuliusBrussee/cavekit`, frozen). every scaffoldable template (`vite`, `game-2d`, `game-3d`, `game-3d-complex`, `jekyll`) + the bare `none` path ship (a) a starter `SPEC.md` carrying §G §C §I §V §T §B in that FIXED order, §T/§B (and §R when present) as pipe tables carrying the Markdown delimiter row (`---|---|---|---`) directly under the header, §V one `- Vn:` bullet per invariant — ⊥ bare `id|…` header w/o delimiter / bare `Vn:` lines: Browse & GitHub merge them into one paragraph (B24), and (b) an `AGENTS.md` whose first three `##` sections are, in order, `Workflow rule: commit + push every turn` → `Workflow rule: git worktrees` → `Workflow rule: the spec is the memory (SDD)`. ORDER is part of the invariant — an agent reads top-down & stops early. the same three head this repo's own `AGENTS.md`. MAINTENANCE protocol (the part cavekit leaves implicit): §V/§I describe the PRESENT ∴ edited, §T/§B are LOGS ∴ append-only; ids monotonic & NEVER reused, even after retirement; a changed rule is REVISED in place at its existing number, tagged `(revised)`, carrying `⊥ <old rule>` (V21, V29, V31, V58, V61); a dead concern is DELETED w/ the retirement logged in the §T row that did the work; a duplicated concern is MERGED into the lowest number w/ a one-line pointer left at the emptied address (V30 → V31). compaction past ~500 lines: §T rows whose substance now lives in §V/§I first, then oldest §B — ⊥ drop a live §V/§I line to save tokens.
- V68: the develop-pane `+` opens an AGENT MENU (`lib/term-agents.js`, one item per `TERM_AGENTS` entry) — it ⊥ creates a tab outright. `TERM_AGENTS` ids ⊆ `AGENTS` in `lib/term-sessions.js`, which is the single validator: `POST /api/term-sessions` 400s an unknown agent BEFORE writing the map or shelling out to `sudo` (a typo silently handing you the wrong agent is worse than an error), while `readSessionsMap` NEVER throws on one — a bad/missing/legacy agent on disk normalizes to `claude`. the menu is `position:fixed`, positioned off the `+` button's own rect (the strip x-scrolls, so a menu positioned inside it drifts off its button); one menu at a time — a second `+` click closes it; outside-click and Escape dismiss without picking, Escape returns focus to `+`. closing the LAST tab respawns one running the SAME agent, ⊥ the default. an entry with no usable uuid is dropped on read, not half-loaded.
- V69: a `codex` tab is a full peer of a claude tab — its own `ttyd@<proj>__sN.service`, its own tmux session, its own strip entry — and runs plain `codex` (`$CODEX_BIN`, default `codex` on PATH). ⊥ `--session-id`/`--resume <uuid>`: codex 0.153 has NO flag that preassigns a session id, and its `resume --last` is cwd-scoped ∴ with two codex tabs in one project it would resume the wrong one. ⇒ TMUX IS THE WHOLE PERSISTENCE STORY for a codex tab: it survives ttyd restarts, browser reconnects and every device switch, and a HOST REBOOT starts the tab fresh (a claude tab still resumes by uuid) — `codex resume` inside the pane recovers an old thread by hand. codex tabs also have no `ai-title` on disk ⇒ `title:null` from the API and the strip labels them by id + an `agent-tag` badge. the badge is shown for non-default agents ONLY, so a badge always means "not claude" and a claude tab looks exactly as it always has. `is_fresh` for a codex tab = "a bootstrap prompt is queued" (`.claude-bootstrap.txt` exists), ⊥ "no transcript" — otherwise every reboot would re-send the prompt.
- V70: the `evenhub` template is a vite-family project (V43) that answers to TWO consumers off ONE bundle: the Even App WebView (⇒ glasses) and a plain browser (⇒ installable PWA). DELIVERY = the live proxied URL via `evenhub qr --url https://<host>/<name>/`, ⊥ an `.ehpk` in the dev loop — packing is store-only & needs a RELATIVE base (`vite build --base=./`), since a packed app ⊥ served from `/<name>/`. `base` (vite) = `start_url` = `scope` (manifest) = `proxyPrefix` (sentinel), ONE string — any drift → assets 404 or the install scope drops its own page. `public/sw.js` = pass-through, CACHES NOTHING (same shape as the hub's own): the page is a live view onto a dev server ∴ a cache feeds the glasses a stale bundle that reads as a code bug; it exists solely for installability. companion UI + sw registration happen BEFORE any bridge call ∴ installability ⊥ depends on hardware. a bridge object ⊥ proof of glasses — the SDK installs one on `window` in ANY browser & force-readies it after 100ms, so `waitForEvenAppBridge()` RESOLVES standalone and only the first call across it (`createStartUpPageContainer` → non-zero) reveals the missing host ⇒ `startGlassesPage` → `null`, page says "No glasses"; ⊥ throw (every plain-browser visit would render an error). ⊥ top-level-await the bridge either — w/ no bridge object at all it never settles, hanging the installed PWA blank; `connectBridge()` races it against a timeout. `app.json` `package_id` = `com.example.<NAMESLUG>` — `evenhub pack` demands lowercase alnum dot-segments each starting w/ a letter ∴ a name like `3d-hud` ⊥ usable raw (`nameSlug` → `app3dhud`).
- V71: template trees under `templates/` are UTF-8 TEXT ONLY — `copyTemplate` reads & writes every file as a string ∴ a committed binary (PNG, font, image) arrives corrupted in the scaffolded project. the PWA icon ships as `icon.svg` for this reason. scaffolded PROJECTS have no such limit.
- V72: landing TAG BAR (`#tagbar`, between subtitle & grid) = leading `All` + one chip per DISTINCT badge across every card. every filterable `.card` carries its tags as a JSON array in `data-tags` — the hardcoded Develop/claude-hub cards carry `[]` (any chip hides them, but Admin/Infra are labels ⊥ vocabulary ∴ never chips, V73); the `+`/upload buttons are ⊥ cards ∴ never hide. grouping is CASE-INSENSITIVE (`Game`≡`game`, `3D`≡`3d`): the badge CSS upper-cases tags so the cards already read as one, and a split chip row would hide half the games behind the "other" spelling. chip label = first spelling seen, count = cards carrying it, order = alphabetical by key. click ⇒ cards w/o that tag get `.filtered-out`; click the active chip or `All` ⇒ clear. active key lives in `?tag=` (`replaceState`) ∴ survives the `location.reload()` after create + bookmarkable; a `?tag` nobody carries → `All`, ⊥ an empty grid; card delete re-renders the bar. pure part = `lib/tag-filter.js` (`collectTags`, `hasTag`), spliced into `landing.html` by `serveLanding` at `/* @inject lib/tag-filter.js */` w/ a FUNCTION replacer (a string replacer reads `$` in the source as a pattern) ∴ landing.html is ⊥ byte-static any more — that marker is its ONLY templating hook, and editing `lib/tag-filter.js` needs a restart while `landing.html` itself still doesn't.
- V73: TAG VOCABULARY is small & HUB-DERIVED, ⊥ an enum in code. as of 2026-09-21: `AI`, `Bible`, `Games`, `Glasses`, `Theater` (+ the auto `worktree`). tags are CATEGORIES ⊥ status (no `WIP`/`Stable`). the leak was the onboarding docs — 'short tags like Game, Tool, API, Library, Service plus WIP or Stable' grew a chip per project (20 chips for 20 projects) — so the docs are what steer: `writeBootstrapPrompt` takes `hubTags` = `currentHubTags()` (`collectTags(listManagedProjects())` minus `worktree`, labels) and BOTH flavors say 'the tags already in use on this hub are: …; add a new tag only if none of them does' (empty hub → 'the chip row lists the tags already in use'); all 5 call sites pass it (`bootstrapNoGithub`, `bootstrapClone`, `bootstrapTemplate`, `bootstrapJekyll`, `bootstrapOnboard(dir, name, opts)`); `agentsTemplate()` step 2 + every `templates/*/AGENTS.md.template` card-metadata bullet say 'reuse a tag already on the hub'; template READMEs seed a vocabulary tag (`Games` ×3, `Glasses`) or `[]` (`vite`, `jekyll`, bare `readmeTemplate`), ⊥ `WIP`/`game`/`3d`/`pwa`/`Site`. hardcoded landing cards carry `data-tags='[]'`.
- V74: a terminal key = a tmux session name, `^[A-Za-z0-9][A-Za-z0-9._-]*$` ≤ 128 chars, validated before any shell-out. tmux targets: `has-session -t =<key>`; EVERY pane-targeted command (`send-keys`, `capture-pane`, `display-message`) `-t =<key>:` — exact session, active pane. ⊥ bare `<key>` (tmux prefix-matches session names ∴ `claude-hub` can land in `claude-hub-g2__s1`); ⊥ `=<key>` w/o the colon on pane commands ("can't find pane", B25).
- V75: the glasses relay is INERT for any terminal no glasses client is watching. watched ⇔ `/api/term-capture/<key>` was polled within `TERM_WATCH_TTL_MS` (5 s) AT THE MOMENT the hook's prompt arrives; otherwise `hold()` answers `relay:false` at once and the hook exits 0 with no output = Claude Code's ordinary TUI prompt. a held prompt also ends `relay:false` when the poll lapses (sweep every 500 ms), on `release`, after 540 s (< the hook's 600 s timeout, so the TUI prompt appears instead of a killed hook), or when a newer prompt supersedes it. `stop`/`notification` are never held. the hook exits 0 silently on every failure (no `TMUX`, hub down, non-JSON, unknown event, `GLASSES_RELAY=0`) and its fire-and-forget events carry a 5 s timeout. ⊥ any path in which a hook can block a terminal the glasses are not looking at.
- V76: `term-capture` = the visible pane only (`capture-pane -p -J`, wrapped lines joined), trailing whitespace trimmed, trailing blank rows dropped, `cols`/`rows` from `#{pane_width} #{pane_height}`. ⊥ `-S -` history — Claude Code lives in the alt screen, which has none (B14); older output is reached by V77 then re-captured.
- V77: `term-scroll` types SGR wheel ticks (`\e[<64;10;10M` up/older for negative `lines`, `\e[<65;10;10M` down) via `send-keys -l`; Claude Code's own mouse tracking consumes them ~1 line per tick (verified 2026-09-21: 5 ticks → 4 lines, reversible). capped at 200 ticks per call. ⊥ tmux copy-mode (`[0/0]` in the alt screen, B14).
- V78: STT is its own unit (`stt.service`, python, `~/stt-env`); the hub only proxies `/api/stt` w/ a 60 s cap and answers 503 when it is down — ⊥ a hang, ⊥ whisper inside the node process. the model sits on the GPU only under a gpu-gate lease (`owner: stt`, ttl 300, renewed by use) and is dropped + released after 90 s idle so ollama gets the card back; a gate/CUDA failure degrades to CPU int8, ⊥ a 500.

- V79: v2 file access — ∀ client path → `resolveUnder(PROJECTS_ROOT, raw)` (`lib/v2-paths.js`) before ANY disk call; `..`, NUL, `\` → 400, escape → 403; `''` = the root itself (no project boundary). tests: `test/v2-paths.test.js`, `test/v2-fs.test.js`, `test/v2-routes.test.js`.
- V80: v2 layout = tree of `split{dir,sizes[],children}` / `panel{id,tabs[],active}`; sizes are FRACTIONS summing to 1 (never px); every op in `lib/v2-layout.js` is pure (clones input) & ends in `normalize` (1-child splits collapse, empty non-root panels vanish, same-dir nesting flattens, sizes renormalize); the only empty panel allowed is a lone root (client refills w/ a home tab). `isConsistent` gates every profile PUT. same file runs in the browser (`window.HubLayout`). test: `test/v2-layout.test.js`.
- V81: v2 file tab modes = raw (highlighted source) / view (rendered md, image, pdf, media, live page for html behind a proxy or a `routes` rule) / edit / diff, availability from `classify(name)`; `PUT /api/v2/fs/text` w/ `baseMtime` ≠ disk mtime → 409 + never clobbers (agent wrote meanwhile); diff/log resolve the NEAREST enclosing repo, walking up but never above `PROJECTS_ROOT`; untracked files diff as added. tests: `test/v2-fs.test.js`, `test/v2-routes.test.js`.
- V82: profile = server-side `{layout, tabs, instructions}` under `HUB_STATE_DIR/profiles/<id>/`; `rev` write counter — PUT w/ stale `rev` → 409 (two devices ⊥ silent clobber); layout+tabs validated together (layout naming an unknown tab → 400); WHICH profile a browser uses = localStorage `hub.profile` only, server never picks. `HUB_STATE_DIR` ∉ `PROJECTS_ROOT` (state never browsable/committable). tests: `test/v2-profiles.test.js`, `test/v2-routes.test.js`.
- V83: v2 session = one file `HUB_STATE_DIR/sessions/<id>.json` (`id` `^[a-z0-9]{8}$`, `cwd` rel to root validated per V79, `agent` ∈ {claude,codex,shell}, `uuid`, `profile`); create = file write, ⊥ `sudo systemctl` (one `ttyd-hub.service` serves all via `--url-arg`); DELETE kills tmux `=hub-<id>` then removes the file. legacy `<proj>__sN` tabs listed beside them, never written. tests: `test/v2-sessions.test.js`, `test/v2-routes.test.js`.
- V84: `ttyd-attach-hub.sh <id>` validates the id, reads ONE record via node, `cd`s `PROJECTS_ROOT/<cwd>`, tmux `hub-<id>` (exact `=` match); claude → `--session-id <uuid>` first / `--resume <uuid>` when `~/.claude/projects/<encoded cwd>/<uuid>.jsonl` exists, + `--append-system-prompt-file HUB_STATE_DIR/profiles/<profile>/CLAUDE.md` iff non-empty; codex → bare; shell → `bash -l`; `<id>.prompt` sent once after 4 s (not for shell) then deleted. `ttyd-hub.service` carries `RuntimeDirectoryPreserve=yes` (V5). test: `test/ttyd-attach-hub.test.js`.
- V85: v2 services = discovery ∪ of (a) regular `/etc/systemd/system/*.service` files w/ `User=<hub user>` or `WorkingDirectory=` under `$HOME`, (b) `vite@*`/`jekyll@*` instances, (c) sentinel `extraUnits`, (d) `services.json` names — minus `ttyd*`; start/stop/restart/logs ONLY for a discovered unit (404 otherwise, before any sudo). parsers pure (`parseSystemctlShow`, `parseTailscaleServeStatus`). test: `test/v2-services.test.js`, `test/v2-routes.test.js`.
- V86: v2 render = two layers: `#layout` (strips, gutters, empty bodies) rebuilt from the tree; `#stage` holds one `.tabcontent` per opened tab, absolutely positioned over its panel body by `place()`; tab contents NEVER move in the DOM (an iframe survives split/drag/resize w/o reload); inactive tabs stay mounted, `visibility:hidden`.
- V87: v2 UI is container-responsive — `.tabcontent` is `container-type:size`; home/browser/file bars switch on `@container` widths, not the window. a home tab in a quarter-width panel gets the phone controls.
- V88: v2 narrow mode iff `innerWidth ≤ 0.75 × innerHeight` (wide phone = desktop): `#layout` hidden, one tab fills the workspace, panels listed as groups in the ☰ sheet; drag disabled. every other size = full panel UI.
- V89: v2 shell chrome — HOME is a singleton (deduped) and unclosable, restored first in the top-left panel on load when missing; it is the only launcher (no `+` new-tab buttons anywhere); the profile chip sits at the start of the top-left strip (a top bar exists only in narrow mode); the active tab carries the accent line on TOP and the divider runs under the inactive tabs; one SVG icon set (`Hub.ICONS`): fork = repo folder / +repo, `>_` = terminal, tray = upload, folder/file glyphs; folder icons blue, green when a RUNNING session's cwd is that folder; the terminal dialog lists sessions already open in the folder above the create form; no open-folder-as-tab buttons. test: `test/v2-shell-source.test.js`.
- V90 (revised): session titles. a LIVE claude session = one whose tmux pane Claude's registry (`~/.claude/sessions/<pid>.json`, `lib/claude-registry.js`) names; its `sessionId` is the CURRENT id (a `--resume` or `/clear` mints a new one) and the hub follows it into the record (`sessions.update({uuid})` / the v1 map) so a reboot resumes the right conversation. title = NEWEST by time of {explicit hub `title` (∞), registry `name` when `nameSource ≠ derived` (at `nameSince`), hub titles store (at `at`)}; only when none exist → `readTranscriptTitle`: `<enc>/<uuid>/custom-title.json` (mtime) → a `custom-title` record beats ANY `ai-title` (Claude re-appends `ai-title` every turn) → `ai-title`. ⊥ "hub store outranks the transcript" and ⊥ "latest record wins" — both buried `/rename` (B27). `services/session-title-hook.mjs` (Stop) forks a detached worker and exits in ms; the worker asks Haiku for a 3–7 word title told to KEEP the current one unless the purpose drifted, then `POST /api/v2/titles` keyed by the hook's `session_id` (the live id); a registry name with `nameSource = user` newer than the last auto title is handed to the model as the USER's name to keep verbatim unless the task plainly changed. inert on `SESSION_TITLES=0`, `HUB_TITLE_WORKER=1`, `stop_hook_active`, no transcript, hub down, title < 20 s old. term tabs follow on every `Hub.loadSessions` (20 s poll). tests: `test/v2-titles.test.js`, `test/v2-routes.test.js`.
- V91: Home = three foldable sections — Sessions, Services (+Tailnet), Explorer — headers always visible, click to fold; wide container (≥ 640px) = sessions | services over explorer (grid), narrow = stacked accordion (open sections share the height); fold state per DEVICE (`localStorage hub.home.sections`), never in the profile; first run in a narrow container folds Services. no per-row action buttons in the explorer (toolbar + the file tab's mode bar are the only controls); no rename button (titles come from Claude / the hook). `?profile=<id>` / `#profile=<id>` overrides the stored profile for that page load WITHOUT persisting.
- V92 (revised): session activity + recency come from Claude's registry, not hooks — `status` busy | waiting | idle (`shell` reads idle) for a live claude session, `null` otherwise; `lastActive` = max(registry `updatedAt`, tmux `session_activity`, transcript mtime of the live id, createdAt). ⊥ `session-activity-hook.mjs` + `/api/v2/activity` (T96) — retired the same day: the registry already carries the state, and the hook could never see a session started before it was installed. client: sessions sort by `lastActive` desc; dot = grey stopped / green running / pulsing green busy / amber waiting. `HUB_CLAUDE_SESSIONS_DIR` overrides the registry dir (tests). tests: `test/v2-titles.test.js` (parser), `test/v2-routes.test.js` (live round trip w/ a real tmux session).
- V93: no row-level buttons anywhere in Home. a new tab lands in the panel with the LARGEST area (measured `.panel-body` rects; narrow mode → the current panel). clicking a service opens a `service` tab: mode `site` when it serves a URL, else `unit` (`GET /api/v2/services/<unit>/unit` = the FragmentPath file, template file for instances); the tab's bar has Site / Unit / Logs (logs poll every 3 s while shown) + Start|Stop, Restart; services sort by this device's last visit (`localStorage hub.services.visits`) then name. ending a session = the term tab's context menu (right-click / long-press). test: `test/v2-routes.test.js` (unit endpoint 404 gate).
- V94: an open v2 page syncs titles/activity via `Hub.loadSessions` at load, every 20 s while visible and on `visibilitychange`; `GET /api/v2/version` = newest mtime across `v2/*` + `lib/v2-layout.js`, polled every 60 s and on visibility — a change reloads the page after flushing the layout, unless an editor is dirty (toast instead). ⊥ a page running yesterday's client forever (B28). tests: `test/v2-routes.test.js`, `test/v2-shell-source.test.js`.

## §T TASKS

| id | status | task | cites |
|---|---|---|---|
| T1 | x | Node http proxy + landing | I.routes |
| T2 | x | `/api/projects` GET/POST/DELETE | I.routes,V13 |
| T3 | x | `/api/view-tree` w/ lazy `?path=` | V7 |
| T4 | x | `/view/*` markdown+code+raw render | V3,V6 |
| T5 | x | two-pane viewer shell, splitter, tabs | I.routes |
| T6 | x | tab persistence per project (localStorage) | V15 |
| T7 | x | develop pane (terminal iframe) toggle + splitter | - |
| T8 | x | HTML eye-icon → `?raw=1` render tab | V16 |
| T9 | x | `/ws/view-tree/<proj>` live updates | V9,V14 |
| T10 | x | watcher emits add/delete in-place, no full reload | V10,V12 |
| T11 | x | watcher emits `change` → iframe reload preserves scroll | V10,V11 |
| T12 | x | services moved to `services/` | I.files |
| T13 | x | tailscale skill split out of AGENTS.md | I.files |
| T14 | x | tests: route validation + path traversal | V1,V2,V3 |
| T15 | x | tests: WS lifecycle + dedup events | V9,V10,V12 |
| T16 | x | tests: tab key collision (`render:` prefix vs path) | V15 |
| T17 | x | watcher recursive on Linux Node 24 — failure-mode test (inotify limit) | V9 |
| T18 | x | CI/lint pipeline: ESLint flat config + GH Actions Node 22 | C.deps |
| T19 | x | add `templates/vite/` w/ React+TS scaffold + `<NAME>`/`<PORT>` placeholders | I.files,V21 |
| T20 | x | add `services/vite@.service` template (`npm run dev`, Restart=always) | I.files,V25 |
| T21 | x | port allocator: scan free port ≥ 5173 not in any project's `.project-meta.json` | V22 |
| T22 | x | `bootstrapVite(dir, name, port)` — copy template, replace placeholders, write meta, `npm install`, enable `vite@<name>` | V21,V23,V24 |
| T23 | x | `POST /api/projects` accepts `template` field; default `'vite'`, dispatch `bootstrapVite` vs `bootstrapNoGithub` | I.routes,V21 |
| T24 | x | `landing.html` create dialog: template select (Vite default \| None) | I.routes |
| T25 | x | `handleDeleteProject` — `extraUnits` already covers `vite@<name>.service` once meta stamped | V26 |
| T26 | x | update README.md + AGENTS.md w/ Vite-default workflow + install steps for `vite@.service` | I.files |
| T27 | x | WS reconnect handler force-reloads all open tabs via `reloadTabFrame` | V11,V27 |
| T28 | x | per-tab scroll persisted to localStorage; restored on iframe load | V28 |
| T29 | x | landing.html: reorder dialog — GitHub above Template. Template fieldset disabled when `gh-mode === 'clone'` (radios + visual fade) | I.routes |
| T30 | x | `handleCreateProject`: force `template = 'none'` when `gh.mode === 'clone'`. ⊥ scaffolder run on cloned repo | V21,V29 |
| T31 | x | `bootstrapClone`: keep "skip if exists" guards on AGENTS.md/README.md; add test asserting pre-existing files survive | V29 |
| T32 | x | bootstrap-prompt branching: write `.claude-bootstrap.txt` per project (scan-existing for clone, greenfield for skip/create); `ttyd-attach.sh` reads + sends + deletes | V30,V31 |
| T33 | x | `GET /api/gh/repos` route w/ in-process 10 min TTL cache. 503 on `gh` error | I.routes,V32 |
| T34 | x | landing.html: clone-source becomes async `<select>`; fallback to `<input>` on fetch fail / empty | V33 |
| T35 | x | tests: cache hit/miss + JSON shape (mock `gh` via stubbed `execFileP`) | V32 |
| T36 | x | README + AGENTS note: forking workflow for non-owned repos | V34 |
| T37 | x | `/api/gh/repos`: filter out candidates whose basename matches any folder under `PROJECTS_ROOT` | I.routes,V32 |
| T38 | x | landing.html: reorder GitHub radios (Clone default, Onboard added). Template fieldset `display:none` when mode ∈ {clone, onboard} | V33,V35 |
| T39 | x | tests: gh-repos filter excludes folder-name matches | V32 |
| T40 | x | server: `bootstrapOnboard(dir, name)` + `GET /api/projects/orphans` + dispatch in `handleCreateProject` for `github.mode === 'onboard'` | I.routes,V36 |
| T41 | x | landing.html: Onboard option populates select from `/api/projects/orphans`; disabled w/ hint when empty | V37 |
| T42 | x | tests: orphan listing + onboard happy path + 409 / 404 errors | V36,V37 |
| T43 | x | gh-repos sort: non-forks first then forks, updatedAt desc within group | V32 |
| T44 | x | rip lazy in-process ttyd. project create runs `sudo -n systemctl enable --now ttyd@<name>.service`; delete unconditionally disables it. /term/<key>/ proxies `unix:/run/ttyd/<key>.sock` directly. develop+shell admin units enabled at install time | V13,V36,I.routes |
| T45 | x | move test-only helpers out of `server.js` exports: `effectiveTemplate`, `writeBootstrapPrompt`, `filterReposByFolders`, `bootstrapOnboard`, `listOrphanFolderNames` → dedicated `lib/*.js` modules. tests import from `lib/`. server.js exports stay at `{server, PROJECT_ID_RE, RESERVED_PROJECT_NAMES, projectWatchers}` | I.exports |
| T46 | x | landing.html onboard hint = exact spec string `"no orphan folders under ~/projects"` | V37 |
| T47 | x | spec dedup: drop §C lines duplicating V2/V3/V6/V7/V21/V22; refresh ttyd socket constraint to /run/ttyd/ 0755; rewrite `POST /api/projects` + `DELETE` narrative for T44 reality; refresh §I.files lib enumeration; trim V24 cleanup-dup, V33 hidden-fieldset-dup, V36 ttyd-enable-dup; merge V30 into V31 | - |
| T48 | x | responsive develop-pane orientation: side-by-side when `vw > 1.2 * vh`, stacked otherwise. listen `resize`, swap flex-direction + splitter axis. separate persisted size keys per orientation | V38 |
| T49 | x | touch→wheel translator on develop iframe: on `load`, listen `touchstart/touchmove` on `contentWindow.document`, dispatch `WheelEvent('wheel', {deltaY, deltaMode:0})` per move delta. preventDefault on touchmove to suppress page scroll inside iframe | V40 |
| T50 | x | extract reload-target selector → `lib/tab-reload-targets.js` (`isEmbedder` + `tabsToReload`). server `handleChange` uses it; inlined into client via `.toString()`. tests cover direct match + embedder transitive reload | V41 |
| T51 | x | inline `EMBED_EXT` const into `isEmbedder` body so `.toString()` round-trip stays self-contained; add Function-reconstruction test | V42 |
| T52 | x | restructure view shell `<main>` into column flex w/ `.top-row` (tree+work-area) + `develop-splitter` + `develop-pane` siblings. drop V38 side-by-side orientation logic and `lib/orientation.js`. develop-pane spans full `<main>` width. structural HTML test asserts develop-pane sibling-of-top-row | V38 |
| T53 | x | eye-icon render mode routes through proxy when project has `proxyTarget`. `renderViewShell` reads `.project-meta.json`, injects `PROXY_PREFIX` const. `openTab` render branch: if PROXY_PREFIX → iframe = `<prefix>/<relpath>` (index.html → trailing slash); else → `?raw=1`. view mode unchanged | V16 |
| T54 | x | add `templates/game-2d/` (Phaser), `templates/game-3d/` (R3F+Three), `templates/game-3d-complex/` (Babylon). each vite, `<NAME>`/`<PORT>`, demo scene, mirror `templates/vite/` file set + `AGENTS.md.template`/`README.md.template` | I.files,V43 |
| T55 | x | add `templates/_firebase/` overlay tree (`src/firebase.ts`, `.env.example`, `firebase.json`, `.firebaserc.template`, README cloud section) | I.files,V45 |
| T56 | x | generalize `bootstrapVite` → `bootstrapTemplate(dir, name, templateId, {firebase})`; id→dir 1:1; stamp `meta.template=templateId`; firebase overlay + `npm install firebase` when flag set | V43,V44,V45 |
| T57 | x | `lib/template-policy.js`: `effectiveTemplate(body)` enum allowlist + coerce-to-vite + clone/onboard force; add `firebaseEnabled(body, template)` | V43,V45 |
| T58 | x | `handleCreateProject`: dispatch any non-`none` template → `bootstrapTemplate`; pass `firebase` flag; force `none`/false on clone/onboard | I.routes,V43,V45 |
| T59 | x | `landing.html`: template radios → 5 options; `Firebase backend` checkbox enabled iff template ≠ None; payload `{template, firebase}` | I.routes,V43,V45 |
| T60 | x | static-deploy: `build:pages`/`build:firebase` scripts in each template `package.json` + `.github/workflows/pages.yml.template` (game/vite trees); `firebase.json` via `_firebase` overlay | V46 |
| T61 | x | tests: effective-template enum+coerce+firebase-forcing; `copyTemplate` each new tree + overlay; routes payload accept/validate | V43,V44,V45 |
| T62 | x | README + AGENTS: game-template catalog + per-host (GH Pages vs Firebase Hosting) deploy steps | I.files |
| T63 | x | `lib/term-sessions.js` — sessions map io + `allocateTabId` (gap-fill) + `parseTermKey`/`joinTermKey` | I.files,V47 |
| T64 | x | `ttyd-attach.sh` — parse `<project>__<tabId>`, lookup uuid, `claude --session-id` (first) / `--resume` (restart); s1-only bootstrap-prompt | V48 |
| T65 | x | server: `GET/POST/DELETE /api/term-sessions/<proj>` + `PUT /active`; sudo enable/disable ttyd@<proj>__sN.service; rollback map on enable fail | I.routes,V47,V49 |
| T66 | x | server: startup `migrateLegacyTermUnits()` — extract uuid from jsonl mtime sort, rename tmux, swap systemd unit; idempotent; admin units exempt | V50 |
| T67 | x | server: `handleDeleteProject` enumerates `ttyd@<proj>__*` + bare unit, batch disable + tmux kill each | V51 |
| T68 | x | server: project create stamps `.develop-sessions.json` w/ `{s1:uuid, lastActive:'s1'}` + enables `ttyd@<proj>__s1.service` instead of bare; `lookupActiveTermKey(name)` shared helper for card/shell termUrl | V47 |
| T69 | x | renderViewShell: tab strip in develop pane (label + ×) + trailing +; one iframe per session (display:none inactive) — switch is instant + state preserved; close-last auto-spawns; initial focus = lastActive (or first); PUT /active on switch | V47,V49 |
| T70 | x | tests: lib/term-sessions roundtrip + allocateTabId gap-fill + parseTermKey edge cases; routes GET/PUT/405 (POST/DELETE skipped — real sudo) | V47,V49 |
| T71 | x | `templates/jekyll/` (Ruby/minima) + `services/jekyll@.service` + `bootstrapJekyll`/`scaffoldProject` dispatch + template-policy enum/firebase-off + landing radio + STACK blurb + tests + docs | I.files,V43,V52 |
| T72 | x | eye-icon renders `.svg`: `isSvgFile`/`isRenderable` in view shell, gate proxy-render branch to `isHtmlFile` so svg → `?raw=1` (image/svg+xml inline); tests | V16 |
| T73 | x | `routes` in `.project-meta.json`: `lib/file-routes.js` (`matchGlob`+`routeForPath`) + `readProjectRoutes` validation + inject `ROUTES` into shell + preview eye-icon on routable files + render-iframe `PROXY_PREFIX+route`; jekyll template default rules; genesis + systematic-theology metas; tests | I.files,V54 |
| T74 | x | extract `lib/readme-meta.js` + `lib/project-cards.js` from `listManagedProjects`; sentinel-over-README precedence, `worktree` badge, worktree-under-parent sort; tests | I.files,V55 |
| T75 | x | `lib/worktree.js` + `handleDeleteProject` git-aware teardown (`worktree remove --force`, `prune` fallback, name validation); landing card provenance line (`⑂ worktree of <parent> · <branch>`, amber badge, anchor link to parent card); tests incl. real-git repro | I.routes,V56,B16 |
| T76 | x | split the two page shells out of server.js → `lib/view-shell.js` + `lib/pwa-shell.js` + shared `lib/escape-html.js`; server.js 4301 → 2452 lines; add first `/p/<proj>/` integration coverage | I.files |
| T77 | x | PWA shell: home link in the term tab strip; FAB gains a third single-pane state (VIEW = the Browse shell) so the rotation is always TERM→OPEN→VIEW, and swaps the right half while split; long-press opens a menu (target-aware Refresh + sticky per-device/per-project Split toggle, the sole way in/out of split) instead of refreshing outright | V57,V58,V59,V60 |
| T78 | x | `lib/android-input.js` — Android-gated capture-phase input shim replacing xterm's lossy IME path (synchronous prefix-diff, codepoint-accurate DEL, `xtermOwns` one-sender-per-key rule + keypress suppression, focusin seeding, boundary-gated tail-trim); de-thrash `lib/keyboard-fit.js` (dedupe no-op vv resizes, rAF-coalesce); wire `ANDROID_INPUT_INJECT` into the ttyd proxyRes blob; 29+5 tests incl. V42 round-trip | V61,V62,B17,B18 |
| T79 | x | `lib/term-reconnect.js` — hold ttyd's close listener to get reconnect backoff, drop its `error` listener so `doReconnect` survives, refit via `window.term.fit` after reopen | V63,V64,B19 |
| T80 | x | README `## On your phone (the PWA)` — `/p/<proj>/`, FAB TERM→OPEN→VIEW rotation, long-press menu, sticky split pref, tab-strip home link; Develop bullet corrected (card links the PWA shell, ⊥ `/term/` direct); landing.html hardcoded claude-hub card blurb refreshed. §I drift: create-dialog template radio order (Jekyll added, None LAST, Vite default) + firebase gating `{none, jekyll}`; `/p/<proj>/` + PWA asset routes added; five ttyd `<head>` shims enumerated in §I.files. eslint browser/serviceworker globals for `upload-dialog.js` + `assets/sw.js` (CI lint was red on main) | I.routes,I.files,V57,V58,V59,V60 |
| T81 | x | `lib/scaffold-install.js` — `installCommand`/`installEnv`; `bootstrapTemplate` passes both to the install `execFileP` (B20). `allowedHosts: ['.ts.net', 'localhost']` in all four vite-family templates (B21). `startFixture({seed})` so a test can stamp a `.project-meta.json` BEFORE `STATIC_ROUTES` is built. tests: `test/scaffold-install.test.js` runs a REAL npm install against `file:` deps (offline, ~0.2s) incl. a negative control that the pre-fix command still reproduces, plus a source guard on the call site; `test/scaffolded-project-serves.test.js` drives a stub upstream carrying the template's own parsed `allowedHosts` through the proxy on a tailnet `Host` (raw `http.request` — `fetch` cannot set Host) | V65,V66,B20,B21 |
| T82 | x | `lib/split-layout.js` — `makeSplitLayout`, the PWA shell's keyboard-immune split verdict; `lib/pwa-shell.js` inlines it (V42) and re-judges on `resize` instead of listening to the `(min-width: 900px) and (orientation: landscape)` media query's `change`. tests: `test/split-layout.test.js` (keyboard/rotation sequences incl. the self-correcting rotate-while-typing case, the desktop corner, V42 round-trip); `test/pwa-shell.test.js` pins the inlined judge + no live `matchMedia`. reproduced before/after headless via CDP `Emulation.setDeviceMetricsOverride` | V58 (revised),B22 |
| T83 | x | `lib/android-input.js` — `focusin` re-seeds the mirror from the live textarea and resets `composing`/`adoptNextInput`, pairing with xterm's blur-time `value = ''` (B23). tests: five `B23:` cases in `test/android-input.test.js`, four of which reproduce against the pre-fix source (DEL storm after a blur, the same after a trim, a composition the blur left open, a pending xterm-owned key swallowing the first input); xterm's `_handleTextAreaBlur` read out of the ttyd-served bundle to confirm the clear | V61 (revised),B23 |
| T84 | x | `SDD.md` — the spec-driven-development protocol: section reference, compressed encoding + symbol table, the loop, backprop, and the maintenance rules (revise / retire / merge, drift sweep, size budget). the three `Workflow rule:` sections head `AGENTS.md`, all five `templates/*/AGENTS.md.template` and server.js's `agentsTemplate()`; starter `SPEC.md.template` in all five templates (real §C/§V per stack) + `specTemplate()` for the bare path, written by `bootstrapNoGithub`; `SPEC.md` added to the jekyll `_config.yml` exclude list; both bootstrap prompts route through the spec — greenfield fills §G/§C & flips §T.1, scan-existing DISTILLS one from the cloned tree. tests: `test/sdd-docs.test.js` scaffolds every template and pins section presence AND order in both docs, greps the two inline server.js templates (the one place it can drift silently), and pins SDD.md's three maintenance moves | V67,V21 (revised),V29 (revised),V31 (revised) |
| T85 | x | SDD tables render: delimiter row under every pipe-table header in `SDD.md`'s examples, all five `templates/*/SPEC.md.template` and server.js `specTemplate()`; §V one `- Vn:` bullet per invariant across the same set; `SDD.md` states the rule under "The file" and shows it in the §V example. tests: `test/sdd-docs.test.js` pins delimiter rows + bullets in every scaffolded SPEC, the bare path, and SDD.md's own examples | V67 (revised),B24 |
| T86 | x | per-tab AGENT choice: the develop `+` opens a Claude/Codex menu (`lib/term-agents.js`, inlined into both shells per V42) instead of creating a claude tab outright. `.develop-sessions.json` entry widens `sN: uuid` → `sN: {uuid, agent}`, legacy bare strings read as claude and are normalized on the next write; `POST /api/term-sessions` takes `{agent?}` and 400s an unknown one ahead of the map write + `sudo`; `GET` reports `agent` and skips the claude-only title read for codex tabs; `ttyd-attach.sh` block-scoped sed probes + a `codex` branch; codex tabs carry an `agent-tag` badge. tests: `test/term-agents.test.js` (fake-DOM menu behaviour incl. dismiss paths + V42 round-trip, both shells' inlined wiring, offer ⊆ validator), `test/ttyd-attach.test.js` — FIRST coverage of the attach script, stub tmux captures the command line for claude fresh/resume, legacy bare-string, codex, s1-vs-s2 and s1-vs-s10 block isolation, missing tab, legacy bare key — plus agent cases in `test/term-sessions.test.js` and 400/404 in `test/term-sessions-routes.test.js` | V47 (revised),V48 (revised),V68,V69 |
| T87 | x | `evenhub` template — Even Realities G2 glasses app in the create dialog. `templates/evenhub/` (vite+TS+`@evenrealities/even_hub_sdk` 0.0.15, `src/glasses.ts` SDK half / `src/main.ts` phone half, `public/{manifest.webmanifest,sw.js,icon.svg}`, `app.json`), seeded from the official `even-realities/evenhub-templates` minimal starter the `/everything-evenhub:template` skill degits, then reshaped for the hub: proxy base, tailnet `allowedHosts`, PWA wiring, no packing step. `<NAMESLUG>` var added to `copyTemplate`'s call (`lib/template.js` `nameSlug`) for the `package_id`; id added to `lib/template-policy.js`, a `STACK` blurb to `lib/bootstrap-prompt.js`, a radio to `landing.html`. verified live: scaffold → `npm install` → typecheck → build → dev server through the prefix (page/manifest/sw/icon 200, tailnet Host 200, unknown Host 403) → Chrome load w/ sw activated at scope `/<name>/`. that run is what caught the bridge-detection bug (the SDK hands out a bridge in any browser; the page rendered "Error createStartUpPageContainer failed (1)" until the refusal became a `null` return). tests: `test/evenhub-template.test.js` (placeholder sweep, base==scope==start_url, cache-free sw, null-not-throw contract, no-pack-script, `package_id` rules, `nameSlug`), plus `evenhub` folded into `test/sdd-docs.test.js`, `test/scaffolded-project-serves.test.js` and `test/effective-template.test.js` | V43 (revised),V70,V71 |
| T88 | x | landing TAG FILTER — first step of organizing the tile page. `lib/tag-filter.js` (`collectTags`/`hasTag`, self-contained), `serveLanding` splices them into `landing.html` at the `@inject` marker (function replacer), `landing.html` gets `#tagbar` + `.chip` styles, `data-tags` on every card (hardcoded ones inline, managed ones from `p.tags`), `?tag=` persistence, delete re-render. tests: `test/tag-filter.test.js` (case-insensitive grouping, first-spelling label, per-card dedupe, sort, junk input, chip↔card agreement, no free identifiers) + `test/landing.test.js` (served page carries both helpers, `#tagbar`, no bare marker, every hardcoded card has `data-tags`) | I.routes,V72 |
| T89 | x | tag vocabulary collapse: 5 tags (`AI`,`Bible`,`Games`,`Glasses`,`Theater`) + `worktree`. every managed README retagged in place; worktree cards inherit the PARENT's tags (`inheritWorktreeTags`, `lib/project-cards.js`) so the three `world-builder-opus-5_*` branches need no README commit each; hardcoded cards → `data-tags='[]'`; `tagsGuidance(hubTags)` in `lib/bootstrap-prompt.js` + `currentHubTags()` in server.js wired into all 5 bootstrap call sites (`bootstrapOnboard` grew an `opts` passthrough); `agentsTemplate` step 2 + 6 `AGENTS.md.template` card-metadata bullets rewritten; 6 `README.md.template` defaults + `readmeTemplate()` reset. tests: `test/tag-vocabulary.test.js` (no status/legacy tag seeded by any template or the bare path; every onboarding doc says 'already on the hub'; every call site passes `hubTags`), `test/bootstrap-prompt.test.js` (+list / empty-hub fallback / no status flag), `test/project-cards.test.js` (+inherit / untagged parent / orphan fallback) | V55 (revised),V72,V73 |
| T90 | x | glasses relay routes: `lib/term-relay.js` + `/api/term-capture`, `/api/term-input`, `/api/term-scroll`, `/api/term-pending[/answer]`, `/api/stt` proxy; tests `test/term-relay.test.js` (pure, incl. watcher-gone/timeout/supersede) + `test/term-relay-routes.test.js` (real throwaway tmux session) | V74,V75,V76,V77,V78 |
| T91 | x | `services/glasses-relay-hook.mjs` + `services/install-glasses-hooks.mjs`; installed into `~/.claude/settings.json` on this box 2026-09-21; ESLint `.mjs` module override | V75 |
| T92 | x | `services/stt/server.py` + `services/stt.service` (faster-whisper turbo, gpu-gate lease, idle unload, CPU fallback); venv `~/stt-env` | V78 |
| T93 | ~ | HUB v2 (`/v2/`, linked from the landing page as "Hub v2"; v1 pages untouched): profiles (`lib/v2-profiles.js`), sessions from any folder via one `ttyd-hub.service` + `services/ttyd-attach-hub.sh` (`lib/v2-sessions.js`), systemd/tailnet service discovery + actions (`lib/v2-services.js`), rootless file API w/ raw/view/edit/diff (`lib/v2-fs.js`, `lib/lang-map.js` split out of server.js), layout tree (`lib/v2-layout.js`, browser-shared), router (`lib/v2-routes.js`, one early hook in server.js), client `v2/{index.html,app.css,app.js,tabs.js,tab-home.js,tab-file.js}` — proportional splits w/ draggable gutters, tab drag → edge/centre/strip/workspace-edge, narrow mode w/ tab groups, CodeMirror 6 editor (esm.sh, textarea fallback) w/ `claude -p` FIM completion (Ctrl+Space; Tab keeps / Esc drops), unified-diff view vs HEAD/any commit, new-repo dialog reusing `POST /api/projects`, upload via `/api/upload-anywhere`. fixture isolates `HUB_STATE_DIR`. tests: `test/v2-{layout,paths,fs,profiles,sessions,services,routes}.test.js`, `test/ttyd-attach-hub.test.js`. LEFT: glasses (Omni) client for the v2 API, touch tab-drag on tablets is best-effort, `/v2/` → `/` swap when v1 retires | V79–V88 |
| T94 | x | v2 shell polish after first review: profile chip into the top-left strip + top bar narrow-only, `+` buttons gone (Home pinned + singleton, `ensureNotEmpty` restores/folds it), active-tab accent on top, SVG icon vocabulary (`Hub.icon`/`Hub.iconPlus` in `v2/tabs.js`), repo folders wear the fork + PROJECT badge dropped, live-session folders green (`Hub.loadSessions` cache shared by the browser and the terminal dialog), terminal dialog lists what already runs in the folder, open-folder-as-tab buttons removed. tests: `test/v2-shell-source.test.js` | V89 |
| T95 | x | second review round: `?profile=` / `#profile=` override (not persisted); explorer row buttons + session rename button removed; HOME accordion (`.sec` sections, per-device folds, grid ≥ 640px); auto-titling — `lib/v2-titles.js` store + `POST/GET/DELETE /api/v2/titles`, `readSessionTitle` honours `custom-title`, `lib/session-title.js` transcript digest + prompt, `services/session-title-hook.mjs` (Stop, detached worker, Haiku) + `services/install-title-hook.mjs` (installed on this box 2026-09-22), term tabs resync titles from `Hub.loadSessions`. B26 caught in the first live run. tests: `test/v2-titles.test.js`, titles round-trip in `test/v2-routes.test.js` | V90,V91 |
| T96 | x | third review round: largest-panel targeting; activity hook + pulsing/amber dots; sessions by recency (`lastActive` from tmux/transcript/hooks), services by last visit; service tab (Site/Unit/Logs + Start/Stop/Restart) replacing every row button and the logs sheet; term-tab context menu for End session; `install-session-hooks.mjs` (installed on this box 2026-09-22) | V92,V93 |
| T97 | x | `/rename` invisible on the tab → read Claude's live-session registry (`lib/claude-registry.js`): current session id (followed into hub + v1 records), `name`/`nameSource`/`nameSince`, `status`; title chosen by recency across registry / hub store; `readTranscriptTitle` (custom-title.json → custom record → ai-title) for stopped sessions; activity hook + endpoint + `lib/v2-activity.js` retired, installer reduced to the title hook and re-run on this box | V90 (revised),V92 (revised),B27 |
| T98 | x | stale-page guard: immediate + visibility title sync, `/api/v2/version` + self-reload (`checkVersion`) | V94,B28 |

## §B BUGS

| id | date | cause | fix |
|---|---|---|---|
| B1 | 2026-05-05 | `nohup node server.js` orphan binds 8002 → systemd unit fail w/ `EADDRINUSE` | V18 |
| B2 | 2026-05-05 | single ttyd stop wipes shared `/run/ttyd/`, orphans peer sockets | V5 |
| B3 | 2026-05-05 | stock `http.server.HTTPServer` wedges → `CLOSE_WAIT` pile, no response | V19 |
| B4 | 2026-05-05 | Vite app asset URLs collapse w/o `base` + `stripPrefix:false` | V20 |
| B5 | 2026-05-05 | `claude --continue` on fresh project → exit-loop (no prior session) | V4 |
| B6 | 2026-05-05 | tab key `'render:' + path` collides w/ filename literally `render:foo` (Linux allows `:` in names) | V15 |
| B7 | 2026-05-05 | WS dropped on server restart; client reconnect backoff 1–30s; edits during gap → no `change` events → tab stale | V27 |
| B8 | 2026-05-08 | ttyd-attach.sh fell back to bare `claude`; absent from systemd PATH → tmux new-session command fails → session dies → exit-loop on every browser connect | V39 |
| B9 | 2026-05-09 | browse tab not auto-reload when image/js referenced by an open `.md`/`.html` tab changes; `handleChange` filtered by exact path match only → user had to close+reopen tab | V41 |
| B10 | 2026-05-09 | V41 fix shipped broken: `isEmbedder` referenced module-scope `const EMBED_EXT` that didn't survive `.toString()` inline → `ReferenceError` aborted `handleChange` for every embedder-mismatch tab → no reload at all on the-first-step/sparks.md edit | V42 |
| B11 | 2026-05-10 | develop-pane nested inside `work-area` (sibling of viewer-pane) → terminal width clipped to area below the file-tabs row instead of spanning full `<main>`. classic side-by-side orientation logic (V38 v1) baked the nesting in | V38 (revised) |
| B12 | 2026-05-14 | eye-icon on `lifebot/index.html` → white screen. Vite source-template index.html refs absolute `<script src="/src/main.tsx">`; iframe origin = claude-hub root, so request lands at `/src/main.tsx` (not under `/view/...`), 404; browser can't transpile TSX anyway → `<div id="root">` stays empty. raw bytes only work for self-contained static HTML; build-tool entry points need the project's running server | V16 (amended) |
| B13 | 2026-07-04 | mobile swipe scrollback broken/erratic: V40 v1 dispatched 1 wheel per touchmove (~60Hz). tmux mouse on → outer xterm in alt buffer, zero local scrollback → xterm forwards each wheel as 1 mouse tick (deltaY magnitude ignored) → tmux copy-mode 5 lines/tick → 1 swipe ≈ 300-line leap into old history; reverse swipe overshoots bottom, `copy-mode -e` exits → felt like "not scrolling" | V40 (revised: quantize 1 tick per 5-line px step) |
| B14 | 2026-07-04 | post-B13 swipe → tmux copy-mode `[0/0]`, no scroll. Claude Code TUI runs in tmux ALT screen (`alternate_on=1`) and requests mouse tracking (`mouse_all_flag=1`, SGR): transcript scrollback lives INSIDE claude, alt screen contributes zero tmux history → copy-mode empty. user `~/.tmux.conf` `bind -n WheelUpPane copy-mode -e` hijacked every wheel into copy-mode instead of tmux's default conditional passthrough (`if pane_in_mode∨mouse_any_flag → send -M`), so claude never received scroll ticks | custom binding removed from `~/.tmux.conf` (default passthrough restored, live-rebound); `ttyd-attach.sh` now sets `mouse on` itself (self-contained, no ~/.tmux.conf dependency) |
| B15 | 2026-08-02 | post-B14 swipe scrolled ~5× too slow. B13's quantization sized the step to tmux copy-mode's 5-lines-per-tick, but after B14 the tick is passed THROUGH to Claude Code, which scrolls **1 line per tick** — so a 5-line step bought 1 line of content per 5 lines of finger. halving to 2.5 lines (first attempt) only reached 2/5. step ! be sized to whoever actually consumes the tick | V40 (revised: 1-line step ⇒ true 1:1) |
| B16 | 2026-08-30 | project DELETE `rm -rf`s a git-worktree folder without telling the parent repo — parent keeps the `.git/worktrees/<dir>` entry, `git worktree list` shows a gone checkout, and `worktree add` refuses to reuse the path | V56 |
| B17 | 2026-08-31 | typing fast on Android's soft keyboard skips characters, as if each one waits on the server. xterm.js routes every Gboard char (`keydown` keyCode 229) into `CompositionHelper._handleAnyTextareaChanges`, which snapshots `textarea.value` and diffs it in a `setTimeout(0)` — (a) the macrotask competes with the renderer, and tmux repainting a full-screen TUI per echoed byte starves the timer queue; (b) the callback is gated on `!_isComposing`, so a composition opening between the keydown and the timer DISCARDS the diff outright, and the composed text that follows covers only `[start,end)` so the earlier char is gone. no local echo ⇒ a dropped char simply never appears. claude-hub amplified it: `installKeyboardFit` forwarded EVERY vv 'resize' (incl. Gboard's no-op suggestion-strip toggles) as a synthetic `window` 'resize', which ttyd turns into an undebounced `fitAddon.fit()` | V61 (shim owns the Android input path), V62 (de-thrash keyboard-fit) |
| B18 | 2026-08-31 | post-V61 Android autocorrect DOUBLED the word. `stopImmediatePropagation` stops propagation, NOT the browser's default action — suppressing `keydown` still lets Chrome fire `keypress` and `input`, and xterm's own listeners are live on the textarea. v1 handed every non-229 key to xterm and only FLAGGED `keypress`, so a key Gboard delivers as a real keyCode (how it commits an autocorrected word together with the space) was sent by xterm's `_keyDown` AND again by the shim's diff. Measured on the live bundle via `term.onData` (which sees BOTH senders, unlike `term.input` which sees only the shim — the flaw in the original verification): a printable `keydown` alone makes xterm emit the char, and a following `keypress` makes it emit a second time. also v1 trimmed the scratch textarea mid-word, desyncing Gboard's composing region | V61 (revised: `xtermOwns` ownership rule, keypress suppression, boundary-gated trim) |
| B19 | 2026-08-31 | a terminal that lost its connection sat on `Press ⏎ to Reconnect` until a keystroke, and came back the shape it had BEFORE the drop. two independent ttyd behaviours: `error → doReconnect = false` (any real network loss errors first), and `dispose()` tearing down the refit binding before reconnecting so the handshake resends stale cols/rows. fixed by V63 + V64 | V63,V64 |
| B20 | 2026-08-31 | every new vite project scaffolded dead: `vite@<name>.service` crash-looped on `sh: 1: vite: not found`. `claude-hub.service` sets `Environment=NODE_ENV=production`; `bootstrapTemplate`'s `npm install` runs as a child and inherits it, and npm treats `NODE_ENV=production` as `--omit=dev` — so ONLY `dependencies` installed and the entire toolchain (vite, typescript, `@vitejs/plugin-react`, `@types/*`) was skipped. npm exited 0, so the scaffold reported success and the catch/`rmSync` cleanup never ran; nothing surfaced until the dev server failed to start. claude-home-automation reached 121 restarts before anyone looked, with exactly three packages in `node_modules`: react, react-dom, scheduler | V65 |
| B21 | 2026-08-31 | a scaffolded project 403'd on the tailnet URL — `Blocked request. This host … is not allowed.` — while answering normally on `127.0.0.1:8002`. none of the four vite templates set `server.allowedHosts`, and the hub forwards the client's `Host` verbatim (`changeOrigin: false`), so Vite 7 rejected the MagicDNS name. every hand-built project (interstellar, test-lab, world-builder, world-builder-opus-5) already carried the setting; the templates never picked it up, so only NEW projects were affected — which is why it survived so long | V66 |
| B22 | 2026-09-05 | on a portrait tablet ≥ 900 CSS px wide the PWA shell jumped into split — half-width terminal, Browse on the right — every time the soft keyboard opened, and when it closed fell to the RIGHT-half pane (Browse), not the terminal being typed in. the split verdict was a live `(min-width: 900px) and (orientation: landscape)` media query; under the shell's `interactive-widget=resizes-content` the keyboard shrinks the layout viewport, and `orientation` is a layout-viewport feature, so 924×1480 → 924×820 reads as landscape. the per-project split pref (V58) masks it wherever split was ever toggled, which is why only a NEW project showed it | V58 (revised) |
| B23 | 2026-09-06 | on Android, the first character typed after returning to a terminal deleted a line or two of what was already at the prompt. xterm's `_handleTextAreaBlur` does `this.textarea.value = ''`, so every blur emptied the scratch textarea while V61's mirror still held everything typed since the last trim (up to 512 chars, ≥ 64 after a trim); `focusin` only re-seeded when the ELEMENT changed, so on return the shim diffed the new char against the stale mirror and sent one DEL per stale char. invisible with an empty prompt — the DELs stop at the prompt — which is why it read as "typing eats my line" rather than as a stuck backspace. leaving a terminal is routine here (the FAB cycles panes, Develop has tabs, the keyboard opens and closes), so it fired constantly. desktop and iOS were never affected: the shim is UA-gated | V61 (revised) |
| B24 | 2026-09-06 | scaffolded `SPEC.md` §R/§T/§B were bare `id\|…` pipe tables w/ no delimiter row & §V bare `Vn:` lines ∴ Markdown (Browse, GitHub) ran each section together into one paragraph; `SDD.md`'s own examples taught the same shape | V67 (revised) |
| B25 | 2026-09-21 | `tmux send-keys -t =<key>` → "can't find pane" while `has-session -t =<key>` succeeded: `=` exact match needs the trailing `:` on pane-targeted commands. caught by `test/term-relay-routes.test.js` before release | V74 |
| B26 | 2026-09-22 | the title hook's worker never ran: the hook spawned it with `HUB_TITLE_WORKER=1` — the same env the worker sets on its `claude -p` child so THAT session's Stop hook stays quiet — and the top-of-file guard quit the worker before its `--work` branch. one variable was doing two jobs. the hook exited 0 in 20 ms so nothing surfaced until the title endpoint stayed 404. fix: the worker runs with the plain env; only the `claude -p` child gets the flag | V90 |
| B27 | 2026-09-22 | `/rename` in the hub's own develop tab never reached the tab title. three causes stacked: (1) the tab's stored uuid was the LAUNCH id, but `/clear` (and `--resume`) had moved the live conversation to a new session id, so every title/activity lookup read the wrong transcript; (2) Claude re-appends `ai-title` after every turn, so "latest record wins" let the stale AI name bury the `custom-title`; (3) this Claude Code version writes the rename to `<uuid>/custom-title.json` + the registry, and the hub store outranked the transcript anyway. found by the user running `/rename` in this very session. fix: T97 | V90 (revised) |
| B28 | 2026-09-22 | the user's tab still read the folder name after the title fix landed: their page had loaded before the client learned to sync titles at all, and nothing ever told an open page that `v2/app.js` had changed — the service worker caches nothing, so a reload was all it needed, but a dashboard that stays open for days needs to reload itself | V94 |
